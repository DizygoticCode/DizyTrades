"use client";
import { useEffect,useState } from "react";import type { FlowRenderStore } from "./lib/order-flow/render-store";import { buildDomRows } from "./lib/order-flow/dom";
const compact=(v:number)=>new Intl.NumberFormat("en-US",{notation:"compact",maximumFractionDigits:2}).format(v);
export function DizyFlowDom({store,contractSize,onClose}:{store:FlowRenderStore;contractSize:number;onClose:()=>void}){const [tick,setTick]=useState(0);useEffect(()=>{let timer:ReturnType<typeof setTimeout>|null=null;return store.subscribe(()=>{if(timer)return;timer=setTimeout(()=>{timer=null;setTick(v=>v+1)},150)})},[store]);const snapshot=store.getSnapshot(),settings=snapshot.settings.dom,{asks,bids}=buildDomRows(snapshot.book,contractSize,settings.levels),bestAsk=snapshot.book.asks[0]?.price,bestBid=snapshot.book.bids[0]?.price,mid=bestAsk&&bestBid?(bestAsk+bestBid)/2:0,max=Math.max(1,...asks.map(v=>v.notional),...bids.map(v=>v.notional));void tick;const rows=(items:typeof asks,side:"ask"|"bid")=>items.map(row=><div className={`dom-row ${side}`} key={`${side}:${row.price}`}><i style={{width:`${row.notional/max*100}%`,background:side==="bid"?settings.bidColour:settings.askColour,opacity:settings.barOpacity}}/><b>{row.price.toLocaleString()}</b><span>{settings.units==="usdt"?compact(row.notional):settings.units==="base"?compact(row.contractQuantity*contractSize):compact(row.contractQuantity)}</span><span>{settings.cumulative?compact(row.cumulativeNotional):""}</span></div>);return <aside className={`dizyflow-dom ${settings.density}`} style={{background:settings.backgroundColour}}><header><b>DEPTH OF MARKET</b><button onClick={onClose} aria-label="Close depth of market">×</button></header><div className="dom-head"><span>Price</span><span>{settings.units}</span><span>{settings.cumulative?"Cumulative":""}</span></div><div className="dom-book">{rows(asks,"ask")}<div className="dom-mid" style={{color:settings.midpointColour}}><b>{mid?mid.toLocaleString():"Warming up…"}</b><span>Spread {bestAsk&&bestBid?(bestAsk-bestBid).toLocaleString():"—"}</span><span>Imbalance {snapshot.book.valid?"live":"—"}</span></div>{rows(bids,"bid")}</div></aside>}
