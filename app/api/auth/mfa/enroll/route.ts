import {NextResponse} from "next/server";import {requireApiUser} from "../../../../lib/auth";import {beginMfaEnrollment,getAccountProfile,verifyAccountPassword} from "../../../../lib/auth-db";import {validRequestOrigin} from "../../../../lib/request-security";import {appendAudit} from "../../../../lib/store";
export async function POST(request:Request){if(!validRequestOrigin(request))return NextResponse.json({error:"Invalid request."},{status:403});const user=await requireApiUser();if(!user||getAccountProfile(user).credentialSource!=="database")return NextResponse.json({error:"Unauthorised"},{status:401});let body:{password?:unknown};try{body=await request.json()}catch{return NextResponse.json({error:"Re-authentication required."},{status:400})}if(typeof body.password!=="string"||!await verifyAccountPassword(user.id,body.password))return NextResponse.json({error:"Re-authentication failed."},{status:403});let secret:string;try{secret=beginMfaEnrollment(user.id)}catch(error){if(error instanceof Error&&error.message==="MFA_ALREADY_ACTIVE")return NextResponse.json({error:"MFA is already active."},{status:409});throw error}await appendAudit(user.id,"auth.mfa-enrollment-begun");return NextResponse.json({secret,otpauth:`otpauth://totp/DizyTrades:${encodeURIComponent(user.email)}?secret=${secret}&issuer=DizyTrades&algorithm=SHA1&digits=6&period=30`});}
