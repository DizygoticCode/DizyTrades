import {requireApiUser} from "../../../../lib/auth";
import {getDepthCollector,normalizeDepthSymbol} from "../../../../lib/order-flow/depth-collector";
export const dynamic="force-dynamic";export const runtime="nodejs";
const encode=(event:string,value:unknown)=>`event: ${event}\ndata: ${JSON.stringify(value)}\n\n`;
export async function GET(request:Request){if(!await requireApiUser())return new Response("Unauthorised",{status:401});const symbol=normalizeDepthSymbol(new URL(request.url).searchParams.get("symbol")??"");if(!symbol)return new Response("Invalid contract symbol",{status:400});const collector=getDepthCollector(symbol),encoder=new TextEncoder();let cleanup=()=>{};const stream=new ReadableStream<Uint8Array>({start(controller){let closed=false;const send=(value:string)=>{if(!closed)controller.enqueue(encoder.encode(value))},latest=collector.getLatest();if(latest)send(encode("envelope",latest));const unsubscribe=collector.subscribe(envelope=>send(encode("envelope",envelope))),keepalive=setInterval(()=>send(": keepalive\n\n"),15_000);cleanup=()=>{if(closed)return;closed=true;unsubscribe();clearInterval(keepalive);request.signal.removeEventListener("abort",cleanup);try{controller.close()}catch{}};request.signal.addEventListener("abort",cleanup,{once:true})},cancel(){cleanup()}});return new Response(stream,{headers:{"content-type":"text/event-stream; charset=utf-8","cache-control":"no-cache, no-transform","x-accel-buffering":"no","connection":"keep-alive"}})}
