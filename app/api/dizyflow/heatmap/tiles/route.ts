import {NextResponse} from "next/server";
import {requireApiUser} from "../../../../lib/auth";
import {normalizeDepthSymbol} from "../../../../lib/order-flow/depth-collector";
import {acquireLiquidityTape,releaseLiquidityTape} from "../../../../lib/order-flow/liquidity-tape";
import type {LiquidityTileResponse} from "../../../../lib/order-flow/types";
export const dynamic="force-dynamic";export const runtime="nodejs";
const number=(value:string|null,min:number,max:number)=>{const parsed=Number(value);return value!==null&&Number.isFinite(parsed)&&parsed>=min&&parsed<=max?parsed:null};
const cache=new Map<string,{value:LiquidityTileResponse;bytes:number}>();let cacheBytes=0;const MAX_CACHE_BYTES=12*1024*1024;
const remember=(key:string,value:LiquidityTileResponse)=>{const bytes=Buffer.byteLength(JSON.stringify(value));if(bytes>MAX_CACHE_BYTES)return;cache.set(key,{value,bytes});cacheBytes+=bytes;while(cacheBytes>MAX_CACHE_BYTES){const oldest=cache.keys().next().value;if(oldest===undefined)break;const item=cache.get(oldest)!;cache.delete(oldest);cacheBytes-=item.bytes}};
export async function GET(request:Request){if(!await requireApiUser())return NextResponse.json({error:"Unauthorised"},{status:401});const p=new URL(request.url).searchParams,symbol=normalizeDepthSymbol(p.get("symbol")??""),now=Date.now(),fromMs=number(p.get("fromMs"),0,now+86_400_000),toMs=number(p.get("toMs"),0,now+86_400_000),minPrice=number(p.get("minPrice"),1e-8,1e9),maxPrice=number(p.get("maxPrice"),1e-8,1e9),timeBucketMs=number(p.get("timeBucketMs"),5_000,600_000),priceStep=number(p.get("priceStep"),1e-8,1e7);if(!symbol||fromMs===null||toMs===null||minPrice===null||maxPrice===null||timeBucketMs===null||priceStep===null||toMs<=fromMs||toMs-fromMs>24*60*60_000||maxPrice<=minPrice)return NextResponse.json({error:"Invalid tile parameters"},{status:400});const key=[symbol,Math.floor(fromMs/5000)*5000,Math.ceil(toMs/5000)*5000,minPrice,maxPrice,timeBucketMs,priceStep].join(":");const hit=cache.get(key);if(hit){cache.delete(key);cache.set(key,hit);return NextResponse.json(hit.value,{headers:{"cache-control":"private, no-store","x-dizyflow-tile-cache":"hit"}})}let tape;try{tape=acquireLiquidityTape(symbol)}catch{return NextResponse.json({error:"Liquidity tape capacity reached"},{status:503})}try{const value=await tape.tiles(fromMs,toMs,minPrice,maxPrice,timeBucketMs,priceStep);remember(key,value);return NextResponse.json(value,{headers:{"cache-control":"private, no-store","x-dizyflow-tile-cache":"miss"}})}finally{releaseLiquidityTape(symbol)}}
