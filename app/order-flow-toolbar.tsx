"use client";import {useSyncExternalStore,useState} from "react";import type {OrderFlowSettings} from "./lib/order-flow/settings";import type {FlowSummary} from "./lib/order-flow/use-order-flow";import type {FlowRenderStore} from "./lib/order-flow/render-store";
const clock=(value:number|null)=>value?new Date(value).toISOString().slice(11,19)+" UTC":"—",money=(value:number)=>`$${value.toLocaleString(undefined,{maximumFractionDigits:2})}`,duration=(start:number|null)=>start?`${Math.floor((Date.now()-start)/60000)}m capture`:"not capturing";
export function OrderFlowToolbar({settings,onChange,summary,onHistory,onRetry,renderStore}:{settings:OrderFlowSettings;onChange:(settings:OrderFlowSettings)=>void;summary:FlowSummary;onHistory:()=>void;onRetry:()=>void;renderStore:FlowRenderStore}){const[diagnostics,setDiagnostics]=useState(false),renderer=useSyncExternalStore(renderStore.subscribeDiagnostics,renderStore.getDiagnostics,renderStore.getDiagnostics);const toggle=(key:"enabled"|"heatmapVisible"|"bubblesVisible"|"domVisible"|"alertsVisible"|"imbalanceVisible")=>onChange({...settings,[key]:!settings[key]});return <div className="dizyflow-controls"><button className={`dizyflow-master ${settings.enabled?"active":""}`} aria-pressed={settings.enabled} onClick={()=>toggle("enabled")} title="Toggle DizyFlow public-data subscription"><b>DIZYFLOW</b><span>{settings.enabled?summary.status:"Off"}</span></button><div className="flow-component-toggles" role="group" aria-label="DizyFlow components">{[["heatmapVisible","Heatmap"],["bubblesVisible","Bubbles"],["domVisible","DOM"],["alertsVisible","Alerts"],["imbalanceVisible","Imbalance"]].map(([key,label])=><button key={key} aria-pressed={settings[key as keyof OrderFlowSettings] as boolean} onClick={()=>toggle(key as "heatmapVisible")} title={`${label}: show or hide without discarding captured data`}>{label}</button>)}<button className="flow-history-button" onClick={onHistory} title="Open bounded alert history">History</button><button aria-expanded={diagnostics} onClick={()=>setDiagnostics(v=>!v)}>Diagnostics</button></div><small>{settings.imbalanceVisible&&summary.imbalance!==null?`${summary.imbalance>0?"+":""}${summary.imbalance.toFixed(0)}% · `:""}{money(summary.spread)} spread · {duration(summary.captureStarted)}</small>{diagnostics?<div className="flow-diagnostics"><span>Depth transport <b>{summary.wsState}</b></span><span>Symbol <b>{summary.activeSymbol}</b></span><span>Server snapshot / local <b>{summary.snapshotVersion} / {summary.version}</b></span><span>Buffered <b>{summary.bufferedUpdates}</b></span><span>Snapshot bids / asks <b>{summary.snapshotBids} / {summary.snapshotAsks}</b></span><span>Book bids / asks <b>{summary.bookBids} / {summary.bookAsks}</b></span><span>Heatmap retained / visible <b>{summary.heatmapCellsRetained} / {summary.heatmapCellsVisible}</b></span><span>Depth received / applied / gaps <b>{summary.depthMessagesReceived} / {summary.depthMessagesApplied} / {summary.versionGaps}</b></span><span>Last recovery error <b>{summary.lastRecoveryError??"none"}</b></span><span>Last snapshot <b>{clock(summary.lastValidUpdate)}</b><br/><span>Snapshot age <b>{summary.lastValidUpdate?`${(summary.latencyMs/1000).toFixed(1)}s`:"—"}</b></span></span><span>Last deal <b>{clock(summary.lastTradeEvent)}</b></span><span>Recovery attempts <b>{summary.recoveryAttempts}</b></span><button type="button" onClick={onRetry}>Retry feed</button><span>Gap <b>{summary.currentGap??"none"}</b></span><span>REST / WS / duplicates <b>{summary.restTradesLoaded} / {summary.dealsReceived} / {summary.duplicatesRejected}</b></span><span>Accepted / rejected / below threshold <b>{summary.dealsAccepted} / {summary.rejectedTimestamps} / {summary.belowThresholdDeals}</b></span>{summary.lastUpstreamError?<span className="negative">Upstream <b>{summary.lastUpstreamError}</b></span>:null}<span>Primitive / enabled <b>{String(renderer.primitiveAttached)} / {String(renderer.renderEnabled)}</b></span><span>Heatmap / bubbles visible <b>{String(renderer.heatmapVisible)} / {String(renderer.bubblesVisible)}</b></span><span>Paint calls / candles <b>{renderer.paintCallCount} / {renderer.candleCount}</b></span><span>Logical range <b>{renderer.visibleLogicalRange?`${renderer.visibleLogicalRange.from.toFixed(1)}–${renderer.visibleLogicalRange.to.toFixed(1)}`:"—"}</b></span><span>Observations / candidates / projected / drawn <b>{renderer.heatmapObservationsRetained} / {renderer.heatmapCandidateCells} / {renderer.heatmapProjectedCells} / {renderer.heatmapCellsDrawn}</b></span><span>Trades / groups / drawn <b>{renderer.rawTradesRetained} / {renderer.bubbleGroupsProduced} / {renderer.bubblesDrawn}</b></span><span>Bubble rejects threshold / time / price <b>{renderer.bubblesRejectedBelowThreshold} / {renderer.bubblesRejectedByTimeProjection} / {renderer.bubblesRejectedByPriceProjection}</b></span><span>Price step <b>{renderer.currentPriceStep}</b></span><span>Drawing status <b>{renderer.failure??"OK"}</b></span><span>Renderer error <b>{renderer.lastRendererError??"none"}</b></span></div>:null}</div>}
