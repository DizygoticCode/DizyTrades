"use client";import type {OrderFlowSettings} from "./lib/order-flow/settings";import type {FlowSummary} from "./lib/order-flow/use-order-flow";
const money=(value:number)=>`$${value.toLocaleString(undefined,{maximumFractionDigits:2})}`,duration=(start:number|null)=>start?`${Math.floor((Date.now()-start)/60000)}m capture`:"not capturing";
export function OrderFlowToolbar({settings,onChange,summary,onHistory}:{settings:OrderFlowSettings;onChange:(settings:OrderFlowSettings)=>void;summary:FlowSummary;onHistory:()=>void}){const toggle=(key:"enabled"|"heatmapVisible"|"bubblesVisible"|"domVisible"|"alertsVisible"|"imbalanceVisible")=>onChange({...settings,[key]:!settings[key]});return <div className="dizyflow-controls"><button className={`dizyflow-master ${settings.enabled?"active":""}`} aria-pressed={settings.enabled} onClick={()=>toggle("enabled")} title={`Master public-data subscription · snapshot ${summary.snapshotVersion} · local ${summary.version} · websocket ${summary.lastWebSocketVersion} · buffered ${summary.bufferedUpdates} · recovery attempts ${summary.recoveryAttempts}`}><b>DIZYFLOW</b><span>{settings.enabled?summary.status:"Off"}</span></button><div className="flow-component-toggles" role="group" aria-label="DizyFlow components">{[["heatmapVisible","Heatmap"],["bubblesVisible","Bubbles"],["domVisible","DOM"],["alertsVisible","Alerts"],["imbalanceVisible","Imbalance"]].map(([key,label])=><button key={key} aria-pressed={settings[key as keyof OrderFlowSettings] as boolean} onClick={()=>toggle(key as "heatmapVisible")} title={`${label}: show or hide without discarding captured data`}>{label}</button>)}<button className="flow-history-button" onClick={onHistory} title="Open bounded alert history">History</button></div><small>{settings.imbalanceVisible&&summary.imbalance!==null?`${summary.imbalance>0?"+":""}${summary.imbalance.toFixed(0)}% · `:""}{money(summary.spread)} spread · {duration(summary.captureStarted)}</small></div>}
