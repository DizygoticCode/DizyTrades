import { readFile, writeFile } from "node:fs/promises";

const replaceOnce=(source,search,replacement,label)=>{const count=source.split(search).length-1;if(count!==1)throw new Error(`${label}: expected 1 match, found ${count}`);return source.replace(search,replacement)};

{
 const path="app/manual-paper-ticket.tsx";
 let source=await readFile(path,"utf8");
 source=replaceOnce(source,'import { estimateLiquidation, paperAccountSummary } from "./lib/manual-paper-engine";','import { auditPaperLiquidation, paperAccountSummary, type PaperLiquidationAudit } from "./lib/manual-paper-engine";',"ticket engine import");
 source=replaceOnce(source,'import {simulatePaperMarketDepthFill,type PaperDepthFillEvidence} from "./lib/manual-paper-depth";\n','import {simulatePaperMarketDepthFill,type PaperDepthFillEvidence} from "./lib/manual-paper-depth";\nimport {selectMexcContractRiskTier,type PaperRiskTierSnapshot} from "./lib/manual-paper-risk-tiers";\n',"ticket risk import");
 source=replaceOnce(source,'type ReduceOnlyEvidence={enabled:true;calculationMethod:"position-bound-cap";source:"manual-close"|"partial-close"|"reverse"|"flatten-all"|"risk-exit"|"opposite-order-replacement";expectedTradeId:string;expectedSide:"long"|"short";positionQuantityBefore:number;requestedQuantity:number;acceptedQuantity:number;capped:boolean;filledQuantity:number;remainingQuantity:number;result:"closed"|"reduced"};\n','type ReduceOnlyEvidence={enabled:true;calculationMethod:"position-bound-cap";source:"manual-close"|"partial-close"|"reverse"|"flatten-all"|"risk-exit"|"opposite-order-replacement";expectedTradeId:string;expectedSide:"long"|"short";positionQuantityBefore:number;requestedQuantity:number;acceptedQuantity:number;capped:boolean;filledQuantity:number;remainingQuantity:number;result:"closed"|"reduced"};\n',"ticket type anchor");
 source=replaceOnce(source,'  estimatedLiquidation: number;\n','  estimatedLiquidation: number;\n  bankruptcyPrice?: number;\n  riskTier?: PaperRiskTierSnapshot;\n  liquidationAudit?: PaperLiquidationAudit;\n',"ticket position risk fields");
 source=replaceOnce(source,'  reduceOnly?: ReduceOnlyEvidence;\n  fee: number;','  reduceOnly?: ReduceOnlyEvidence;\n  bankruptcyPrice?: number;\n  riskTier?: PaperRiskTierSnapshot;\n  liquidationAudit?: PaperLiquidationAudit;\n  fee: number;',"ticket fill risk fields");
 source=replaceOnce(source,'    notional=contractOrder?.notional??0,\n    margin=','    notional=contractOrder?.notional??0,\n    riskTierState=(()=>{try{return {tier:contract&&contractOrder?selectMexcContractRiskTier(contract,{contractVolume:contractOrder.contractVolume,notional:contractOrder.notional}):null,error:null as string|null}}catch{return {tier:null,error:"Requested size exceeds the documented public MEXC risk schedule."}}})(),\n    riskTierPreview=riskTierState.tier,\n    riskTierIssue=riskTierState.error??(riskTierPreview&&leverageNumber>riskTierPreview.maxLeverage?`Tier ${riskTierPreview.level} allows at most ${riskTierPreview.maxLeverage}× leverage.`:null),\n    margin=',"ticket tier preview");
 source=replaceOnce(source,'    liquidation=quantity>0?estimateLiquidation({side,entryPrice:executionPrice,quantity,marginMode,assignedMargin:margin,crossCollateral:equity,entryFee:fee,maintenanceMarginRate:contract?.maintenanceMarginRate??(account?.settings.maintenanceMarginPct??.5)/100,liquidationPenaltyRate:(account?.settings.liquidationPenaltyPct??.1)/100}):NaN,','    liquidationAudit=(()=>{try{return quantity>0?auditPaperLiquidation({side,entryPrice:executionPrice,quantity,marginMode,assignedMargin:margin,crossCollateral:equity,entryFee:fee,maintenanceMarginRate:riskTierPreview?.maintenanceMarginRate??contract?.maintenanceMarginRate??(account?.settings.maintenanceMarginPct??.5)/100,liquidationPenaltyRate:(account?.settings.liquidationPenaltyPct??.1)/100}):null}catch{return null}})(),\n    liquidation=liquidationAudit?.estimatedLiquidation??NaN,\n    bankruptcy=liquidationAudit?.bankruptcyPrice??NaN,',"ticket liquidation audit");
 source=replaceOnce(source,'    invalidAmount = !Number.isFinite(quantity) || quantity <= 0 || margin < 0 || Boolean(contractVolumeIssue) || invalidPriceStep;','    invalidAmount = !Number.isFinite(quantity) || quantity <= 0 || margin < 0 || Boolean(contractVolumeIssue) || Boolean(riskTierIssue) || invalidPriceStep;',"ticket tier invalid");
 source=replaceOnce(source,'                ["Maintenance margin", contract?`${(contract.maintenanceMarginRate*100).toFixed(3)}%`:"Simulator fallback"],','                ["Risk tier", riskTierPreview?`Tier ${riskTierPreview.level} · ${riskTierPreview.riskLimitType}`:"Unavailable"],\n                ["Tier exposure", riskTierPreview?`${riskTierPreview.exposure.toLocaleString()} / ${riskTierPreview.maxExposure?.toLocaleString()??"unbounded"}`:"—"],\n                ["Tier max leverage", riskTierPreview?`${riskTierPreview.maxLeverage}×`:"—"],\n                ["Maintenance margin", riskTierPreview?`${(riskTierPreview.maintenanceMarginRate*100).toFixed(3)}%`:contract?`${(contract.maintenanceMarginRate*100).toFixed(3)}% fallback`:"Simulator fallback"],\n                ["Tier source", riskTierPreview?.source??"Unavailable"],',"ticket preview tier rows");
 source=replaceOnce(source,'                ["Estimated liquidation", Number.isFinite(liquidation)?money(liquidation):"—"],','                ["Estimated liquidation", Number.isFinite(liquidation)?money(liquidation):"—"],\n                ["Bankruptcy price", Number.isFinite(bankruptcy)?money(bankruptcy):"—"],\n                ["Liquidation buffer", liquidationAudit?money(liquidationAudit.liquidationToBankruptcyDistance):"—"],',"ticket bankruptcy preview");
 source=replaceOnce(source,'              {contractVolumeIssue?<span>{contractVolumeIssue}; requested size cannot be opened.</span>:null}\n','              {contractVolumeIssue?<span>{contractVolumeIssue}; requested size cannot be opened.</span>:null}\n              {riskTierIssue?<span>{riskTierIssue}</span>:null}\n              {riskTierPreview?.source==="mexc-public-contract-flat-fallback"?<span>Public tier increment fields are unavailable; this order uses the explicit flat contract MMR fallback.</span>:null}\n',"ticket tier warnings");
 source=replaceOnce(source,'                          <span>{p.marginMode}<small>Estimated liquidation {money(p.estimatedLiquidation)}</small>{p.pendingRiskExit?<small>{`${p.pendingRiskExit.reason.toUpperCase()} triggered · ${money(p.pendingRiskExit.triggerPrice)} · awaiting visible depth`}</small>:null}</span>','                          <span>{p.marginMode}<small>Tier {p.riskTier?.level??"legacy"} · MMR {p.riskTier?`${(p.riskTier.maintenanceMarginRate*100).toFixed(3)}%`:"legacy"}</small><small>Liquidation {money(p.estimatedLiquidation)} · bankruptcy {p.bankruptcyPrice==null?"—":money(p.bankruptcyPrice)}</small>{p.pendingRiskExit?<small>{`${p.pendingRiskExit.reason.toUpperCase()} triggered · ${money(p.pendingRiskExit.triggerPrice)} · awaiting visible depth`}</small>:null}</span>',"ticket position risk display");
 source=replaceOnce(source,'                          {fill.reduceOnly?<small>{`reduce-only ${fill.reduceOnly.source} · requested ${fill.reduceOnly.requestedQuantity} · filled ${fill.reduceOnly.filledQuantity} · remaining ${fill.reduceOnly.remainingQuantity}${fill.reduceOnly.capped?" · capped":""}`}</small>:null}\n','                          {fill.reduceOnly?<small>{`reduce-only ${fill.reduceOnly.source} · requested ${fill.reduceOnly.requestedQuantity} · filled ${fill.reduceOnly.filledQuantity} · remaining ${fill.reduceOnly.remainingQuantity}${fill.reduceOnly.capped?" · capped":""}`}</small>:null}\n                          {fill.riskTier?<small>{`risk tier ${fill.riskTier.level} · MMR ${(fill.riskTier.maintenanceMarginRate*100).toFixed(3)}% · ${fill.riskTier.source} · liq ${fill.liquidationAudit?money(fill.liquidationAudit.estimatedLiquidation):"—"} · bankruptcy ${fill.bankruptcyPrice==null?"—":money(fill.bankruptcyPrice)}`}</small>:null}\n',"ticket history risk evidence");
 await writeFile(path,source);
}

{
 const path="app/lib/manual-paper-backup.ts";
 let source=await readFile(path,"utf8");
 const anchor='function reduceOnlyEvidence(value:unknown,field:string,fillTradeId:unknown,fillSide:unknown,fillQuantity:unknown):ManualFill["reduceOnly"]{';
 const start=source.indexOf(anchor),next=source.indexOf('\n\nfunction position(',start);
 if(start<0||next<0)throw new Error("backup reduce-only boundary unavailable");
 const insert=`\nfunction riskTierSnapshot(value:unknown,field:string):ManualPosition["riskTier"]{if(value==null)return undefined;const input=object(value,field),snapshot=Object.freeze({symbol:symbol(input.symbol,field+".symbol"),source:oneOf(input.source,field+".source",["mexc-public-contract-derived","mexc-public-contract-flat-fallback","legacy-flat-assumption"] as const),calculationMethod:oneOf(input.calculationMethod,field+".calculationMethod",["contract-tier-increments","flat-contract-fallback","legacy-flat-assumption"] as const),riskLimitType:oneOf(input.riskLimitType,field+".riskLimitType",["BY_VOLUME","BY_VALUE","UNKNOWN"] as const),level:number(input.level,field+".level",1,1000),exposure:number(input.exposure,field+".exposure",0.000000000001),maxExposure:input.maxExposure==null?null:number(input.maxExposure,field+".maxExposure",0.000000000001),maintenanceMarginRate:number(input.maintenanceMarginRate,field+".maintenanceMarginRate",0,0.999999999),initialMarginRate:number(input.initialMarginRate,field+".initialMarginRate",0,0.999999999),maxLeverage:number(input.maxLeverage,field+".maxLeverage",1,1000),baseExposure:input.baseExposure==null?null:number(input.baseExposure,field+".baseExposure",0.000000000001),exposureIncrement:input.exposureIncrement==null?null:number(input.exposureIncrement,field+".exposureIncrement",0.000000000001),maintenanceIncrement:number(input.maintenanceIncrement,field+".maintenanceIncrement",0,0.999999999),initialIncrement:number(input.initialIncrement,field+".initialIncrement",0,0.999999999),levelLimit:number(input.levelLimit,field+".levelLimit",1,1000),capturedAt:number(input.capturedAt,field+".capturedAt",1)}),tol=Math.max(1e-9,snapshot.exposure*1e-9);if(snapshot.level>snapshot.levelLimit)throw new Error(field+" level exceeds its schedule.");if(snapshot.maxExposure!==null&&snapshot.exposure-snapshot.maxExposure>tol)throw new Error(field+" exposure exceeds its tier.");if(snapshot.calculationMethod==="contract-tier-increments"){if(snapshot.source!=="mexc-public-contract-derived"||snapshot.baseExposure===null||snapshot.exposureIncrement===null)throw new Error(field+" has incomplete derived provenance.");const expected=snapshot.baseExposure+snapshot.exposureIncrement*(snapshot.level-1);if(snapshot.maxExposure===null||Math.abs(snapshot.maxExposure-expected)>Math.max(1e-9,expected*1e-9))throw new Error(field+" tier boundary does not reconcile.")}return snapshot}\nfunction liquidationAuditEvidence(value:unknown,field:string,estimated:unknown,bankruptcy:unknown,entry:unknown,quantityValue:unknown):ManualPosition["liquidationAudit"]{if(value==null)return undefined;const input=object(value,field),audit=Object.freeze({calculationMethod:oneOf(input.calculationMethod,field+".calculationMethod",["linear-usdt-mark-notional-v2"] as const),bankruptcyCalculationMethod:oneOf(input.bankruptcyCalculationMethod,field+".bankruptcyCalculationMethod",["linear-usdt-zero-equity-v1"] as const),collateralBasis:oneOf(input.collateralBasis,field+".collateralBasis",["assigned-margin","cross-collateral-snapshot"] as const),collateral:number(input.collateral,field+".collateral",0.000000000001),usableCollateral:number(input.usableCollateral,field+".usableCollateral",0),entryFee:number(input.entryFee,field+".entryFee",0),maintenanceMarginRate:number(input.maintenanceMarginRate,field+".maintenanceMarginRate",0,0.999999999),liquidationPenaltyRate:number(input.liquidationPenaltyRate,field+".liquidationPenaltyRate",0,0.999999999),maintenanceMarginAtLiquidation:number(input.maintenanceMarginAtLiquidation,field+".maintenanceMarginAtLiquidation",0),liquidationPenaltyReserve:number(input.liquidationPenaltyReserve,field+".liquidationPenaltyReserve",0),estimatedLiquidation:number(input.estimatedLiquidation,field+".estimatedLiquidation",0),bankruptcyPrice:number(input.bankruptcyPrice,field+".bankruptcyPrice",0),liquidationToBankruptcyDistance:number(input.liquidationToBankruptcyDistance,field+".liquidationToBankruptcyDistance",0)}),storedEstimated=number(estimated,field+".storedEstimated",0),storedBankruptcy=number(bankruptcy,field+".storedBankruptcy",0),quantity=number(quantityValue,field+".quantity",0.000000000001),tol=Math.max(1e-8,Math.abs(storedEstimated)*1e-9);if(Math.abs(audit.estimatedLiquidation-storedEstimated)>tol||Math.abs(audit.bankruptcyPrice-storedBankruptcy)>Math.max(1e-8,Math.abs(storedBankruptcy)*1e-9))throw new Error(field+" stored prices do not reconcile.");if(Math.abs(audit.collateral-audit.entryFee-audit.usableCollateral)>Math.max(1e-8,audit.collateral*1e-9))throw new Error(field+" collateral does not reconcile.");if(Math.abs(audit.maintenanceMarginAtLiquidation-audit.estimatedLiquidation*quantity*audit.maintenanceMarginRate)>Math.max(1e-8,audit.maintenanceMarginAtLiquidation*1e-9))throw new Error(field+" maintenance margin does not reconcile.");if(Math.abs(audit.liquidationPenaltyReserve-audit.estimatedLiquidation*quantity*audit.liquidationPenaltyRate)>Math.max(1e-8,audit.liquidationPenaltyReserve*1e-9)||Math.abs(audit.liquidationToBankruptcyDistance-Math.abs(audit.estimatedLiquidation-audit.bankruptcyPrice))>tol)throw new Error(field+" liquidation buffer does not reconcile.");return audit}\n`;
 source=source.slice(0,next)+insert+source.slice(next);
 source=replaceOnce(source,'    estimatedLiquidation: number(\n      input.estimatedLiquidation,\n      "manualPaper.position.estimatedLiquidation",\n      0,\n    ),\n    entryFee:','    estimatedLiquidation: number(\n      input.estimatedLiquidation,\n      "manualPaper.position.estimatedLiquidation",\n      0,\n    ),\n    bankruptcyPrice: input.bankruptcyPrice == null ? undefined : number(input.bankruptcyPrice, "manualPaper.position.bankruptcyPrice", 0),\n    riskTier: riskTierSnapshot(input.riskTier, "manualPaper.position.riskTier"),\n    liquidationAudit: liquidationAuditEvidence(input.liquidationAudit, "manualPaper.position.liquidationAudit", input.estimatedLiquidation, input.bankruptcyPrice, input.entryPrice, input.quantity),\n    entryFee:',"backup position risk fields");
 source=replaceOnce(source,'    estimatedLiquidation:\n      input.estimatedLiquidation == null\n        ? undefined\n        : number(input.estimatedLiquidation, "manualPaper.fill.estimatedLiquidation", 0),\n    riskPriceSource:','    estimatedLiquidation:\n      input.estimatedLiquidation == null\n        ? undefined\n        : number(input.estimatedLiquidation, "manualPaper.fill.estimatedLiquidation", 0),\n    bankruptcyPrice: input.bankruptcyPrice == null ? undefined : number(input.bankruptcyPrice, "manualPaper.fill.bankruptcyPrice", 0),\n    riskTier: riskTierSnapshot(input.riskTier, "manualPaper.fill.riskTier"),\n    liquidationAudit: input.liquidationAudit == null ? undefined : liquidationAuditEvidence(input.liquidationAudit, "manualPaper.fill.liquidationAudit", input.estimatedLiquidation, input.bankruptcyPrice, input.entryPrice??input.price, input.quantity),\n    riskPriceSource:',"backup fill risk fields");
 await writeFile(path,source);
}
