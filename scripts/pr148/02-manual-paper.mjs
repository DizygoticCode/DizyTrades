import { readFile, writeFile } from "node:fs/promises";

const replaceOnce=(source,search,replacement,label)=>{const count=source.split(search).length-1;if(count!==1)throw new Error(`${label}: expected 1 match, found ${count}`);return source.replace(search,replacement)};

const path="app/lib/manual-paper.ts";
let source=await readFile(path,"utf8");
source=replaceOnce(source,'import {estimateLiquidation,evaluatePaperClose,selectRiskPrice,sizePaperPosition,type CloseReason,type MarginMode,type PaperSizeMode,type RiskPriceSource} from "./manual-paper-engine";','import {auditPaperLiquidation,estimateLiquidation,evaluatePaperClose,selectRiskPrice,sizePaperPosition,type CloseReason,type MarginMode,type PaperLiquidationAudit,type PaperSizeMode,type RiskPriceSource} from "./manual-paper-engine";',"manual engine import");
source=replaceOnce(source,'import {createManualReduceOnlyPlan,finaliseManualReduceOnly,type ManualReduceOnlyEvidence,type ManualReduceOnlyPlan,type ManualReduceOnlySource,type ManualReduceOnlyTarget} from "./manual-paper-reduce-only";\n','import {createManualReduceOnlyPlan,finaliseManualReduceOnly,type ManualReduceOnlyEvidence,type ManualReduceOnlyPlan,type ManualReduceOnlySource,type ManualReduceOnlyTarget} from "./manual-paper-reduce-only";\nimport {legacyPaperRiskTierSnapshot,reselectPaperRiskTier,selectMexcContractRiskTier,type PaperRiskTierSnapshot} from "./manual-paper-risk-tiers";\n',"risk tier import");
source=replaceOnce(source,'estimatedLiquidation:number;entryFee:number;','estimatedLiquidation:number;bankruptcyPrice?:number;riskTier?:PaperRiskTierSnapshot;liquidationAudit?:PaperLiquidationAudit;entryFee:number;',"position risk types");
source=replaceOnce(source,'estimatedLiquidation?:number;riskPriceSource?:RiskPriceSource;','estimatedLiquidation?:number;bankruptcyPrice?:number;riskTier?:PaperRiskTierSnapshot;liquidationAudit?:PaperLiquidationAudit;riskPriceSource?:RiskPriceSource;',"fill risk types");

const precisionOld='function precisionSnapshot(position:ManualPosition,contractVolume=position.contractVolume){const precision=contractVolume===undefined?{}:{contractVolume,contractSize:position.contractSize,priceUnit:position.priceUnit,volUnit:position.volUnit,minContractVolume:position.minContractVolume,maxContractVolume:position.maxContractVolume};return {...precision,entryDepthFill:position.entryDepthFill}}\n';
const precisionNew=`function precisionSnapshot(position:ManualPosition,contractVolume=position.contractVolume){const precision=contractVolume===undefined?{}:{contractVolume,contractSize:position.contractSize,priceUnit:position.priceUnit,volUnit:position.volUnit,minContractVolume:position.minContractVolume,maxContractVolume:position.maxContractVolume};return {...precision,entryDepthFill:position.entryDepthFill,riskTier:position.riskTier,bankruptcyPrice:position.bankruptcyPrice,liquidationAudit:position.liquidationAudit}}\nfunction positionRiskExposure(position:ManualPosition,tier:PaperRiskTierSnapshot){return tier.riskLimitType==="BY_VOLUME"?(position.contractVolume??(position.contractSize?position.quantity/position.contractSize:position.quantity)):position.quantity*position.entryPrice}\nfunction legacyPositionRiskTier(position:ManualPosition,settings:ManualSettings){return legacyPaperRiskTierSnapshot({symbol:position.symbol,riskLimitType:position.contractVolume!==undefined?"BY_VOLUME":"UNKNOWN",exposure:position.contractVolume??position.quantity*position.entryPrice,maintenanceMarginRate:settings.maintenanceMarginPct/100,initialMarginRate:1/Math.max(1,position.leverage),maxLeverage:Math.max(1,position.leverage),capturedAt:Number.isFinite(Date.parse(position.openedAt))?Date.parse(position.openedAt):Date.now()})}\nfunction positionRiskTier(position:ManualPosition,settings:ManualSettings){return position.riskTier??legacyPositionRiskTier(position,settings)}\nfunction refreshPositionRisk(position:ManualPosition,account:ManualAccount){try{const tier=reselectPaperRiskTier(positionRiskTier(position,account.settings),positionRiskExposure(position,positionRiskTier(position,account.settings))),crossCollateral=position.liquidationAudit?.collateral??manualEquity(account,{[position.symbol]:position.lastRiskPrice}),liquidationAudit=auditPaperLiquidation({side:position.side,entryPrice:position.entryPrice,quantity:position.quantity,marginMode:position.marginMode,assignedMargin:position.margin,crossCollateral,entryFee:position.entryFee,maintenanceMarginRate:tier.maintenanceMarginRate,liquidationPenaltyRate:account.settings.liquidationPenaltyPct/100});return {...position,riskTier:tier,estimatedLiquidation:liquidationAudit.estimatedLiquidation,bankruptcyPrice:liquidationAudit.bankruptcyPrice,liquidationAudit}}catch{return position}}\nfunction validatedEntryRiskTier(contract:MexcContractMetadata,contractVolume:number,notional:number,leverage:number){try{const tier=selectMexcContractRiskTier(contract,{contractVolume,notional});if(leverage-tier.maxLeverage>1e-10)return fail("RISK_TIER_LEVERAGE_EXCEEDED","leverage",\`Tier \${tier.level} allows at most \${tier.maxLeverage}× leverage for this position size.\`);return tier}catch(error){if(error instanceof ManualPaperError)throw error;const code=error instanceof Error?error.message:"INVALID_RISK_TIER";if(code==="RISK_LIMIT_EXCEEDED")return fail(code,"amount","Requested position exceeds the documented public MEXC risk schedule.");return fail("RISK_TIER_UNAVAILABLE","amount","The requested position cannot be mapped to a valid maintenance tier.")}}\n`;
source=replaceOnce(source,precisionOld,precisionNew,"risk helpers");

source=replaceOnce(source,'const postCloseEquity=manualEquity(account,{[input.symbol]:marketPrice}),margin=notional/sizing.leverage,available=postCloseEquity-usedManualMargin(account);if(margin>available)fail("INSUFFICIENT_EQUITY","amount","Margin exceeds available manual paper equity.");const exits=','const postCloseEquity=manualEquity(account,{[input.symbol]:marketPrice}),margin=notional/sizing.leverage,available=postCloseEquity-usedManualMargin(account),riskTier=validatedEntryRiskTier(currentContract,contractVolume,notional,sizing.leverage);if(margin>available)fail("INSUFFICIENT_EQUITY","amount","Margin exceeds available manual paper equity.");const exits=',"entry risk tier");
source=replaceOnce(source,'const orderId=randomUUID(),tradeId=`mp1_${randomUUID().replaceAll("-","")}`,marketKey=`mexc:futures:${input.symbol}`,timestamp=new Date().toISOString(),estimatedLiquidation=estimateLiquidation({side:input.side,entryPrice:slipped,quantity,marginMode,assignedMargin:margin,crossCollateral:postCloseEquity,entryFee:fee,maintenanceMarginRate:currentContract.maintenanceMarginRate,liquidationPenaltyRate:account.settings.liquidationPenaltyPct/100});account.positions[input.symbol]=','const orderId=randomUUID(),tradeId=`mp1_${randomUUID().replaceAll("-","")}`,marketKey=`mexc:futures:${input.symbol}`,timestamp=new Date().toISOString(),liquidationAudit=auditPaperLiquidation({side:input.side,entryPrice:slipped,quantity,marginMode,assignedMargin:margin,crossCollateral:postCloseEquity,entryFee:fee,maintenanceMarginRate:riskTier.maintenanceMarginRate,liquidationPenaltyRate:account.settings.liquidationPenaltyPct/100}),estimatedLiquidation=liquidationAudit.estimatedLiquidation,bankruptcyPrice=liquidationAudit.bankruptcyPrice;account.positions[input.symbol]=',"entry liquidation audit");
source=replaceOnce(source,'margin,marginMode,...exits,estimatedLiquidation,entryFee:fee,','margin,marginMode,...exits,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,entryFee:fee,',"position entry risk snapshot");
source=replaceOnce(source,'takeProfit:exits.takeProfit,estimatedLiquidation,riskPriceSource,entryFee:fee,','takeProfit:exits.takeProfit,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,riskPriceSource,entryFee:fee,',"fill entry risk snapshot");

source=replaceOnce(source,'else account.positions[symbol]={...position,...feeSnapshot,quantity:Number((remainingContractVolume*rules.contractSize).toPrecision(15)),contractVolume:remainingContractVolume,contractSize:rules.contractSize,priceUnit:rules.priceUnit,volUnit:rules.volUnit,minContractVolume:rules.minVol,maxContractVolume:rules.maxVol,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)};return exitDepthFill','else account.positions[symbol]=refreshPositionRisk({...position,...feeSnapshot,quantity:Number((remainingContractVolume*rules.contractSize).toPrecision(15)),contractVolume:remainingContractVolume,contractSize:rules.contractSize,priceUnit:rules.priceUnit,volUnit:rules.volUnit,minContractVolume:rules.minVol,maxContractVolume:rules.maxVol,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)},account);return exitDepthFill',"depth residual risk refresh");
source=replaceOnce(source,'account.positions[symbol]={...position,...feeSnapshot,quantity:Number((position.quantity-requestedQuantity).toPrecision(15)),contractVolume:remainingContractVolume,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)}}}account.idempotencyKeys.push(key);','account.positions[symbol]=refreshPositionRisk({...position,...feeSnapshot,quantity:Number((position.quantity-requestedQuantity).toPrecision(15)),contractVolume:remainingContractVolume,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)},account)}}account.idempotencyKeys.push(key);',"legacy residual risk refresh");
source=replaceOnce(source,'account.positions[symbol]={...old,...feeSnapshot,tradeId,side:openSide,entryPrice:openPrice,entryFee:openFee,margin,stopLoss:null,takeProfit:null,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),openedAt:timestamp};account.idempotencyKeys.push(key);','account.positions[symbol]=refreshPositionRisk({...old,...feeSnapshot,tradeId,side:openSide,entryPrice:openPrice,entryFee:openFee,margin,stopLoss:null,takeProfit:null,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),openedAt:timestamp},account);account.idempotencyKeys.push(key);',"legacy reverse risk refresh");

const openReverseStart='function openReversedDepthPosition(account:ManualAccount,userId:string,old:ManualPosition,key:string,marketPrice:number,openSide:ManualSide,entryDepthFill:PaperDepthFillEvidence,current:MexcContractMetadata){';
const openReverseEnd='}\nexport async function reverseManualPosition';
const start=source.indexOf(openReverseStart),end=source.indexOf(openReverseEnd,start);
if(start<0||end<0)throw new Error("reverse open function boundary unavailable");
const replacement=`function openReversedDepthPosition(account:ManualAccount,userId:string,old:ManualPosition,key:string,marketPrice:number,openSide:ManualSide,entryDepthFill:PaperDepthFillEvidence,current:MexcContractMetadata){const notional=entryDepthFill.notional,feeSnapshot=mexcPublicMarketTakerFeeSnapshot(current),feeBreakdown=paperExecutionFee(notional,feeSnapshot),fee=feeBreakdown.totalFee,margin=notional/old.leverage,available=manualEquity(account,{[old.symbol]:marketPrice})-usedManualMargin(account),riskTier=validatedEntryRiskTier(current,entryDepthFill.filledContractVolume,notional,old.leverage);if(margin>available)fail("INSUFFICIENT_EQUITY","amount","Reversed position exceeds available equity after the close leg.");const timestamp=new Date().toISOString(),tradeId="mp1_"+randomUUID().replaceAll("-",""),liquidationAudit=auditPaperLiquidation({side:openSide,entryPrice:entryDepthFill.executionPrice,quantity:entryDepthFill.quantity,marginMode:old.marginMode,assignedMargin:margin,crossCollateral:manualEquity(account,{[old.symbol]:marketPrice}),entryFee:fee,maintenanceMarginRate:riskTier.maintenanceMarginRate,liquidationPenaltyRate:account.settings.liquidationPenaltyPct/100}),estimatedLiquidation=liquidationAudit.estimatedLiquidation,bankruptcyPrice=liquidationAudit.bankruptcyPrice;account.cashBalance=Math.max(0,account.cashBalance-fee);account.fees+=fee;account.positions[old.symbol]={...old,...feeSnapshot,tradeId,side:openSide,quantity:entryDepthFill.quantity,contractVolume:entryDepthFill.filledContractVolume,contractSize:current.contractSize,priceUnit:current.priceUnit,volUnit:current.volUnit,minContractVolume:current.minVol,maxContractVolume:current.maxVol,entryDepthFill,entryPrice:entryDepthFill.executionPrice,entryFee:fee,margin,stopLoss:null,takeProfit:null,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),lastRiskPrice:marketPrice,openedAt:timestamp};account.fills.push({orderId:randomUUID(),fillId:randomUUID(),tradeId,marketKey:old.marketKey,marketType:old.marketType,idempotencyKey:key,userId,symbol:old.symbol,side:openSide,marginMode:old.marginMode,leverage:old.leverage,price:entryDepthFill.executionPrice,quantity:entryDepthFill.quantity,contractVolume:entryDepthFill.filledContractVolume,contractSize:current.contractSize,priceUnit:current.priceUnit,volUnit:current.volUnit,minContractVolume:current.minVol,maxContractVolume:current.maxVol,entryDepthFill,notional,marginUsed:margin,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,riskPriceSource:old.riskPriceSource,entryFee:fee,...feeSnapshot,tradingFee:feeBreakdown.tradingFee,liquidationPenalty:feeBreakdown.liquidationPenalty,fee,timestamp,realisedPnl:0,resultingBalance:account.cashBalance})}\nexport async function reverseManualPosition`;
source=source.slice(0,start)+replacement+source.slice(end+openReverseEnd.length);

await writeFile(path,source);
