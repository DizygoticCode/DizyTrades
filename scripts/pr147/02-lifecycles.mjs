import { readFile, writeFile } from "node:fs/promises";

const replaceOnce = (source, search, replacement, label) => {
  const count = source.split(search).length - 1;
  if (count !== 1) throw new Error(label + ": expected 1 match, found " + count);
  return source.replace(search, replacement);
};
const replaceFunction = (source, startName, nextName, replacement) => {
  const start = source.indexOf(startName);
  const end = source.indexOf(nextName, start + startName.length);
  if (start < 0 || end < 0) throw new Error("Function boundary missing: " + startName + " -> " + nextName);
  return source.slice(0, start) + replacement + "\n" + source.slice(end);
};

const path = "app/lib/manual-paper.ts";
let source = await readFile(path, "utf8");
source = replaceOnce(
  source,
  'if(existing&&existing.side!==input.side&&!input.confirmReverse)fail("REVERSAL_CONFIRMATION_REQUIRED","side","Reversal confirmation required.");const oppositeDepthPlan=depth&&existing&&existing.side!==input.side?planOppositeDepthReplacement(existing,input.side,sizing.notional,marketPrice,currentContract,depth):null;',
  'if(existing&&existing.side!==input.side&&!input.confirmReverse)fail("REVERSAL_CONFIRMATION_REQUIRED","side","Reversal confirmation required.");const replacementReduceOnlyPlan=existing&&existing.side!==input.side?manualReduceOnlyPlan(existing,existing.quantity,existing.quantity,"opposite-order-replacement",clientReduceOnlyTarget(existing,input.expectedTradeId,input.expectedSide)):undefined,oppositeDepthPlan=depth&&existing&&existing.side!==input.side?planOppositeDepthReplacement(existing,input.side,sizing.notional,marketPrice,currentContract,depth):null;',
  "opposite replacement target",
);
source = replaceOnce(
  source,
  'closeWithDepthAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close",existing!.quantity,depth!,currentContract,"reversal");',
  'closeWithDepthAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close",existing!.quantity,depth!,currentContract,"reversal",replacementReduceOnlyPlan);',
  "partial opposite replacement close",
);
source = replaceOnce(
  source,
  'if(depth&&oppositeDepthPlan?.entryDepthFill)closeWithDepthAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close",existing.quantity,depth,currentContract,"reversal");else closeAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close","manual");',
  'if(depth&&oppositeDepthPlan?.entryDepthFill)closeWithDepthAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close",existing.quantity,depth,currentContract,"reversal",replacementReduceOnlyPlan);else closeAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close","reversal",replacementReduceOnlyPlan);',
  "full opposite replacement close",
);

source = replaceFunction(source, "export async function markManualPosition", "function validateKey", String.raw`export async function markManualPosition(userId:string,symbol:string,riskPrice:number,source:RiskPriceSource,current?:MexcFundingRateSnapshot,history:readonly MexcFundingSettlement[]=[],depth?:DepthEnvelope|null,contract?:MexcContractMetadata|null,requireDepth=false){return serial(userId,async()=>{const account=await readManualAccount(userId),position=account.positions[symbol];if(!position)return account;applyFundingHistory(account,userId,position,riskPrice,source,current,history);position.lastRiskPrice=riskPrice;position.riskPriceSource=source;const evaluated=evaluatePaperClose({side:position.side,riskPrice,stopLoss:position.stopLoss,takeProfit:position.takeProfit,estimatedLiquidation:position.estimatedLiquidation}),reason:ManualRiskExitTrigger["reason"]|null=position.pendingRiskExit?.reason??(evaluated&&isRiskExitReason(evaluated)?evaluated:null);if(reason){if(!position.pendingRiskExit)position.pendingRiskExit={reason,triggeredAt:new Date().toISOString(),triggerPrice:riskPrice,priceSource:source};const reduceOnlyPlan=manualReduceOnlyPlan(position,position.quantity,position.quantity,"risk-exit");if(requireDepth){if(depth&&contract)try{closeWithDepthAt(account,userId,symbol,riskPrice,"auto-"+reason+"-"+Date.now(),position.quantity,depth,contract,reason,reduceOnlyPlan)}catch(error){if(!(error instanceof ManualPaperError&&["DEPTH_STALE","DEPTH_UNAVAILABLE","DEPTH_LIQUIDITY_BELOW_MINIMUM"].includes(error.code)))throw error}}else closeAt(account,userId,symbol,riskPrice,"auto-"+reason+"-"+Date.now(),reason,reduceOnlyPlan)}account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}
`);

source = replaceFunction(source, "export async function partialCloseManualPosition", "async function reverseManualPositionLegacy", String.raw`export async function partialCloseManualPosition(userId:string,symbol:string,key:string,marketPrice:number,input:{percentage?:unknown;quantity?:unknown},depth?:DepthEnvelope,current?:MexcContractMetadata,target?:ManualReduceOnlyTarget){return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,key);const position=account.positions[symbol];if(!position)fail("NO_POSITION","symbol","No manual position.");const closeSize=closeQuantityForInput(position,input),reduceOnlyPlan=manualReduceOnlyPlan(position,closeSize.requestedQuantity,closeSize.acceptedQuantity,"partial-close",target),requestedQuantity=reduceOnlyPlan.acceptedQuantity;if(depth)closeWithDepthAt(account,userId,symbol,marketPrice,key,requestedQuantity,depth,current,"manual",reduceOnlyPlan);else{const rawSlipped=marketPrice*(1+(position.side==="long"?-1:1)*account.settings.slippagePct/100),slipped=position.priceUnit?quantizeMexcExecutionPrice(rawSlipped,position.priceUnit,position.side,false):rawSlipped,notional=requestedQuantity*slipped,feeSnapshot=positionMarketTakerFeeSnapshot(position,account.settings),feeBreakdown=paperExecutionFee(notional,feeSnapshot),fee=feeBreakdown.totalFee,pnl=(slipped-position.entryPrice)*requestedQuantity*(position.side==="long"?1:-1),ratio=requestedQuantity/position.quantity,timestamp=new Date().toISOString(),fullyClosed=ratio>=1-1e-12,allocatedEntryFee=(position.entryFee??0)*ratio,fundingPnl=fullyClosed?(position.fundingPnl??0):0,tradingNet=pnl-allocatedEntryFee-fee,net=tradingNet+fundingPnl,reduceOnly=finaliseManualReduceOnly(reduceOnlyPlan,requestedQuantity);account.cashBalance+=pnl-fee;account.realisedPnl+=tradingNet;account.fees+=fee;account.fills.push({orderId:randomUUID(),fillId:randomUUID(),tradeId:position.tradeId,marketKey:position.marketKey,marketType:position.marketType,idempotencyKey:key,userId,symbol,side:"close",marginMode:position.marginMode,leverage:position.leverage,entryPrice:position.entryPrice,price:slipped,quantity:requestedQuantity,...precisionSnapshot(position,position.contractSize?Number((requestedQuantity/position.contractSize).toPrecision(15)):undefined),reduceOnly,notional,marginUsed:(position.margin??position.quantity*position.entryPrice/position.leverage)*ratio,entryFee:allocatedEntryFee,exitFee:fee,...feeSnapshot,tradingFee:feeBreakdown.tradingFee,liquidationPenalty:feeBreakdown.liquidationPenalty,fee,timestamp,openedAt:position.openedAt,closeReason:fullyClosed?"manual":undefined,grossPnl:pnl,fundingPnl,netPnl:net,realisedPnl:net,resultingBalance:account.cashBalance});if(fullyClosed)delete account.positions[symbol];else{const remainingContractVolume=position.contractVolume===undefined||!position.contractSize?undefined:Number((position.contractVolume-requestedQuantity/position.contractSize).toPrecision(15));account.positions[symbol]={...position,...feeSnapshot,quantity:Number((position.quantity-requestedQuantity).toPrecision(15)),contractVolume:remainingContractVolume,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)}}}account.idempotencyKeys.push(key);account.fills=account.fills.slice(-500);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}
`);

source = replaceFunction(source, "async function reverseManualPositionLegacy", "function mapDepthReverseEntryError", String.raw`async function reverseManualPositionLegacy(userId:string,symbol:string,key:string,marketPrice:number,target?:ManualReduceOnlyTarget){return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,key);const old=account.positions[symbol];if(!old)fail("NO_POSITION","symbol","No manual position.");if(old.pendingRiskExit)fail("RISK_EXIT_PENDING","side","A triggered risk exit must finish before reversing the position.");const reduceOnlyPlan=manualReduceOnlyPlan(old,old.quantity,old.quantity,"reverse",target),rawClosePrice=marketPrice*(1+(old.side==="long"?-1:1)*account.settings.slippagePct/100),closePrice=old.priceUnit?quantizeMexcExecutionPrice(rawClosePrice,old.priceUnit,old.side,false):rawClosePrice,openSide:ManualSide=old.side==="long"?"short":"long",rawOpenPrice=marketPrice*(1+(openSide==="long"?1:-1)*account.settings.slippagePct/100),openPrice=old.priceUnit?quantizeMexcExecutionPrice(rawOpenPrice,old.priceUnit,openSide,true):rawOpenPrice,closeNotional=old.quantity*closePrice,openNotional=old.quantity*openPrice,feeSnapshot=positionMarketTakerFeeSnapshot(old,account.settings),closeFeeBreakdown=paperExecutionFee(closeNotional,feeSnapshot),openFeeBreakdown=paperExecutionFee(openNotional,feeSnapshot),closeFee=closeFeeBreakdown.totalFee,openFee=openFeeBreakdown.totalFee,pnl=(closePrice-old.entryPrice)*old.quantity*(old.side==="long"?1:-1),oldEntryFee=old.entryFee??0,oldFundingPnl=old.fundingPnl??0,oldTradingNet=pnl-oldEntryFee-closeFee,oldNet=oldTradingNet+oldFundingPnl,margin=openNotional/old.leverage;if(margin+openFee>manualEquity(account,{[symbol]:marketPrice})-usedManualMargin(account)+(old.margin??old.quantity*old.entryPrice/old.leverage))fail("INSUFFICIENT_EQUITY","amount","Reverse position exceeds available equity.");const timestamp=new Date().toISOString(),tradeId="mp1_"+randomUUID().replaceAll("-",""),reduceOnly=finaliseManualReduceOnly(reduceOnlyPlan,old.quantity);account.cashBalance+=pnl-closeFee-openFee;account.realisedPnl+=oldTradingNet;account.fees+=closeFee+openFee;account.fills.push({orderId:randomUUID(),fillId:randomUUID(),tradeId:old.tradeId,marketKey:old.marketKey,marketType:old.marketType,idempotencyKey:key+":close",userId,symbol,side:"close",marginMode:old.marginMode,leverage:old.leverage,entryPrice:old.entryPrice,price:closePrice,quantity:old.quantity,...precisionSnapshot(old),reduceOnly,notional:closeNotional,marginUsed:old.margin,entryFee:oldEntryFee,exitFee:closeFee,...feeSnapshot,tradingFee:closeFeeBreakdown.tradingFee,liquidationPenalty:closeFeeBreakdown.liquidationPenalty,fee:closeFee,timestamp,openedAt:old.openedAt,closeReason:"reversal",grossPnl:pnl,fundingPnl:oldFundingPnl,netPnl:oldNet,realisedPnl:oldNet,resultingBalance:account.cashBalance+openFee},{orderId:randomUUID(),fillId:randomUUID(),tradeId,marketKey:old.marketKey,marketType:old.marketType,idempotencyKey:key+":open",userId,symbol,side:openSide,marginMode:old.marginMode,leverage:old.leverage,price:openPrice,quantity:old.quantity,...precisionSnapshot(old),notional:openNotional,marginUsed:margin,entryFee:openFee,...feeSnapshot,tradingFee:openFeeBreakdown.tradingFee,liquidationPenalty:openFeeBreakdown.liquidationPenalty,fee:openFee,timestamp,realisedPnl:0,resultingBalance:account.cashBalance});account.positions[symbol]={...old,...feeSnapshot,tradeId,side:openSide,entryPrice:openPrice,entryFee:openFee,margin,stopLoss:null,takeProfit:null,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),openedAt:timestamp};account.idempotencyKeys.push(key);account.updatedAt=timestamp;await writeManualAccount(userId,account);return account})}
`);

source = replaceFunction(source, "export async function reverseManualPosition", "export async function attachManualHistoricalDizyFlow", String.raw`export async function reverseManualPosition(userId:string,symbol:string,key:string,marketPrice:number,depth?:DepthEnvelope,current?:MexcContractMetadata,target?:ManualReduceOnlyTarget){if(!depth||!current)return reverseManualPositionLegacy(userId,symbol,key,marketPrice,target);return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,key);const old=account.positions[symbol];if(!old)fail("NO_POSITION","symbol","No manual position.");if(old.pendingRiskExit)fail("RISK_EXIT_PENDING","side","A triggered risk exit must finish before reversing the position.");const reduceOnlyPlan=manualReduceOnlyPlan(old,old.quantity,old.quantity,"reverse",target),rules=positionDepthRules(old,current),openContractVolume=quantizeMexcStep(old.contractVolume??old.quantity/rules.contractSize,rules.volUnit,"nearest"),openSide:ManualSide=old.side==="long"?"short":"long";let closePreview:PaperDepthFillEvidence;try{closePreview=simulatePaperMarketDepthFill({side:old.side,opening:false,requestedContractVolume:openContractVolume,openContractVolume,minimumRemainingContractVolume:old.minContractVolume??rules.minVol,referencePrice:marketPrice,contract:rules,depth})}catch(error){return mapDepthExitError(error,rules)}const remaining=closePreview.remainingPositionContractVolume??Math.max(0,openContractVolume-closePreview.filledContractVolume);if(remaining>rules.volUnit*1e-9){closeWithDepthAt(account,userId,symbol,marketPrice,key+":close",old.quantity,depth,current,"reversal",reduceOnlyPlan)}else{let entryDepthFill:PaperDepthFillEvidence;try{entryDepthFill=simulatePaperMarketDepthFill({side:openSide,opening:true,requestedContractVolume:openContractVolume,priorConsumedContractVolume:closePreview.filledContractVolume,referencePrice:marketPrice,contract:rules,depth})}catch(error){return mapDepthReverseEntryError(error,rules)}closeWithDepthAt(account,userId,symbol,marketPrice,key+":close",old.quantity,depth,current,"reversal",reduceOnlyPlan);openReversedDepthPosition(account,userId,old,key+":open",marketPrice,openSide,entryDepthFill,current)}account.idempotencyKeys.push(key);account.fills=account.fills.slice(-500);account.idempotencyKeys=account.idempotencyKeys.slice(-1000);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}

`);

source = replaceFunction(source, "async function flattenManualPositionsLegacy", "export async function flattenManualPositions", String.raw`async function flattenManualPositionsLegacy(userId:string,key:string,prices:Record<string,number>){return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,key);const symbols=Object.keys(account.positions);for(const symbol of symbols)if(!Number.isFinite(prices[symbol])||prices[symbol]<=0)fail("PRICE_UNAVAILABLE","marketPrice","Public price unavailable for "+symbol+".");for(const symbol of symbols){const position=account.positions[symbol];if(position)closeAt(account,userId,symbol,prices[symbol],key+":"+symbol,"manual",manualReduceOnlyPlan(position,position.quantity,position.quantity,"flatten-all"))}account.idempotencyKeys.push(key);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}
`);

source = replaceFunction(source, "export async function flattenManualPositions", "export async function updateManualSettings", String.raw`export async function flattenManualPositions(userId:string,key:string,markets:Record<string,number|{price:number;depth:DepthEnvelope;contract:MexcContractMetadata}>){if(Object.values(markets).every(value=>typeof value==="number"))return flattenManualPositionsLegacy(userId,key,markets as Record<string,number>);return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,key);const symbols=Object.keys(account.positions);for(const symbol of symbols){const market=markets[symbol],price=typeof market==="number"?market:market?.price;if(!Number.isFinite(price)||Number(price)<=0)fail("PRICE_UNAVAILABLE","marketPrice","Public price unavailable for "+symbol+".")}for(const symbol of symbols){const market=markets[symbol],position=account.positions[symbol];if(!position)continue;const reduceOnlyPlan=manualReduceOnlyPlan(position,position.quantity,position.quantity,"flatten-all");if(typeof market==="number")closeAt(account,userId,symbol,market,key+":"+symbol,"manual",reduceOnlyPlan);else try{closeWithDepthAt(account,userId,symbol,market.price,key+":"+symbol,position.quantity,market.depth,market.contract,"manual",reduceOnlyPlan)}catch(error){if(error instanceof ManualPaperError&&error.code==="DEPTH_LIQUIDITY_BELOW_MINIMUM")continue;throw error}}account.idempotencyKeys.push(key);account.fills=account.fills.slice(-500);account.idempotencyKeys=account.idempotencyKeys.slice(-1000);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}

`);

await writeFile(path, source);
