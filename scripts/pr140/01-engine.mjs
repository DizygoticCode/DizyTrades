import { replaceExact, replaceRegex } from "./utils.mjs";

await replaceExact(
  "app/lib/manual-paper.ts",
  `import {calculatePaperFundingPayment,dueMexcFundingSettlements,parseMexcFundingHistory,parseMexcFundingRate,type MexcFundingRateSnapshot,type MexcFundingSettlement} from "./manual-paper-funding";`,
  `import {calculatePaperFundingPayment,dueMexcFundingSettlements,parseMexcFundingHistory,parseMexcFundingRate,type MexcFundingRateSnapshot,type MexcFundingSettlement} from "./manual-paper-funding";\nimport {simulatePaperMarketDepthFill,type PaperDepthFillEvidence} from "./manual-paper-depth";\nimport type {DepthEnvelope} from "./order-flow/types";`
);

await replaceRegex(
  "app/lib/manual-paper.ts",
  /export type ManualPosition=\{.*?\};/s,
  `export type ManualPosition={tradeId:string;marketKey:string;marketType:"futures";symbol:string;side:ManualSide;quantity:number;contractVolume?:number;contractSize?:number;priceUnit?:number;volUnit?:number;minContractVolume?:number;maxContractVolume?:number;entryDepthFill?:PaperDepthFillEvidence;entryPrice:number;leverage:number;margin:number;marginMode:MarginMode;stopLoss:number|null;takeProfit:number|null;estimatedLiquidation:number;entryFee:number;executionType?:PaperExecutionType;liquidityRole?:PaperLiquidityRole;feeRate?:number;feeSource?:PaperFeeSource;makerFeeRate?:number;takerFeeRate?:number;fundingRate?:number;fundingMinRate?:number;fundingMaxRate?:number;fundingCollectCycleHours?:number;nextFundingTime?:number;fundingSource?:"mexc-public-funding-rate";fundingObservedAt?:number;fundingPnl?:number;lastFundingSettlementAt?:number;riskPriceSource:RiskPriceSource;lastRiskPrice:number;openedAt:string};`
);
await replaceRegex(
  "app/lib/manual-paper.ts",
  /export type ManualFill=\{.*?\};/s,
  `export type ManualFill={orderId:string;fillId:string;tradeId?:string;marketKey?:string;marketType?:"futures";historicalDizyFlow?:HistoricalDizyFlowReference;idempotencyKey:string;userId:string;symbol:string;side:ManualSide|"close";marginMode?:MarginMode;leverage?:number;price:number;entryPrice?:number;quantity:number;contractVolume?:number;contractSize?:number;priceUnit?:number;volUnit?:number;minContractVolume?:number;maxContractVolume?:number;entryDepthFill?:PaperDepthFillEvidence;notional:number;marginUsed?:number;stopLoss?:number|null;takeProfit?:number|null;estimatedLiquidation?:number;riskPriceSource?:RiskPriceSource;entryFee?:number;exitFee?:number;executionType?:PaperExecutionType;liquidityRole?:PaperLiquidityRole;feeRate?:number;feeSource?:PaperFeeSource;makerFeeRate?:number;takerFeeRate?:number;tradingFee?:number;liquidationPenalty?:number;fundingPnl?:number;fee:number;timestamp:string;openedAt?:string;closeReason?:CloseReason;grossPnl?:number;netPnl?:number;realisedPnl:number;resultingBalance:number};`
);

await replaceExact(
  "app/lib/manual-paper.ts",
  `function precisionSnapshot(position:ManualPosition,contractVolume=position.contractVolume){return contractVolume===undefined?{}:{contractVolume,contractSize:position.contractSize,priceUnit:position.priceUnit,volUnit:position.volUnit,minContractVolume:position.minContractVolume,maxContractVolume:position.maxContractVolume}}`,
  `function precisionSnapshot(position:ManualPosition,contractVolume=position.contractVolume){const precision=contractVolume===undefined?{}:{contractVolume,contractSize:position.contractSize,priceUnit:position.priceUnit,volUnit:position.volUnit,minContractVolume:position.minContractVolume,maxContractVolume:position.maxContractVolume};return {...precision,entryDepthFill:position.entryDepthFill}}`
);

await replaceRegex(
  "app/lib/manual-paper.ts",
  /export async function submitManualOrder.*?\nfunction closeAt/s,
  `export async function submitManualOrder(userId:string,input:{idempotencyKey:string;symbol:string;side:ManualSide;sizeMode:ManualSizeMode;amount:unknown;leverage?:unknown;marginMode?:MarginMode;stopLoss?:unknown;takeProfit?:unknown;confirmReverse?:boolean},marketPrice:number,riskPriceSource:RiskPriceSource="last",contract?:MexcContractMetadata,funding?:MexcFundingRateSnapshot,depth?:DepthEnvelope){return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,input.idempotencyKey);const marginMode=input.marginMode??"isolated";if(marginMode!=="isolated"&&marginMode!=="cross")fail("INVALID_MARGIN_MODE","marginMode","Choose isolated or cross margin.");if(!contract)fail("CONTRACT_METADATA_UNAVAILABLE","symbol","Current public MEXC contract rules are unavailable.");const currentContract=contract as MexcContractMetadata,equity=manualEquity(account,{[input.symbol]:marketPrice}),sizing=calculateManualSizing({...input,leverage:input.leverage??1,side:input.side,minLeverage:currentContract.minLeverage,maxLeverage:currentContract.maxLeverage},equity,marketPrice),existing=account.positions[input.symbol];if(existing?.side===input.side&&!account.settings.allowAdding)fail("SAME_SIDE_POSITION","side","Same-side position already exists.");if(existing&&existing.side!==input.side&&!input.confirmReverse)fail("REVERSAL_CONFIRMATION_REQUIRED","side","Reversal confirmation required.");let entryDepthFill:PaperDepthFillEvidence|undefined,slipped:number,quantity:number,notional:number,contractVolume:number;if(depth){const requestedOrder=validatedContractOrder(sizing.notional,marketPrice,currentContract);try{entryDepthFill=simulatePaperMarketDepthFill({side:input.side,requestedContractVolume:requestedOrder.contractVolume,referencePrice:marketPrice,contract:currentContract,depth})}catch(error){const code=error instanceof Error?error.message:"DEPTH_UNAVAILABLE";if(code==="DEPTH_STALE")return fail(code,"depth","Public DizyFlow depth is stale.");if(code==="DEPTH_INCOMPLETE"||code==="DEPTH_SYMBOL_MISMATCH")return fail("DEPTH_UNAVAILABLE","depth","A complete public DizyFlow book is unavailable.");if(code==="DEPTH_LIQUIDITY_BELOW_MINIMUM")return fail(code,"amount",\`Visible liquidity cannot fill the minimum \${currentContract.minVol} contracts.\`);return fail("DEPTH_UNAVAILABLE","depth","The requested market entry cannot be represented by current public depth.")}slipped=entryDepthFill.executionPrice;quantity=entryDepthFill.quantity;notional=entryDepthFill.notional;contractVolume=entryDepthFill.filledContractVolume}else{const rawSlipped=marketPrice*(1+(input.side==="long"?1:-1)*account.settings.slippagePct/100);slipped=quantizeMexcExecutionPrice(rawSlipped,currentContract.priceUnit,input.side,true);const legacyOrder=validatedContractOrder(sizing.notional,slipped,currentContract);quantity=legacyOrder.quantity;notional=legacyOrder.notional;contractVolume=legacyOrder.contractVolume}if(existing)closeAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close","manual");const postCloseEquity=manualEquity(account,{[input.symbol]:marketPrice}),margin=notional/sizing.leverage,available=postCloseEquity-usedManualMargin(account);if(margin>available)fail("INSUFFICIENT_EQUITY","amount","Margin exceeds available manual paper equity.");const exits=validateExits(input.side,slipped,input.stopLoss,input.takeProfit,currentContract.priceUnit),feeSnapshot=mexcPublicMarketTakerFeeSnapshot(currentContract),feeBreakdown=paperExecutionFee(notional,feeSnapshot),fee=feeBreakdown.totalFee;account.cashBalance-=fee;account.fees+=fee;const orderId=randomUUID(),tradeId=\`mp1_\${randomUUID().replaceAll("-","")}\`,marketKey=\`mexc:futures:\${input.symbol}\`,timestamp=new Date().toISOString(),estimatedLiquidation=estimateLiquidation({side:input.side,entryPrice:slipped,quantity,marginMode,assignedMargin:margin,crossCollateral:postCloseEquity,entryFee:fee,maintenanceMarginRate:currentContract.maintenanceMarginRate,liquidationPenaltyRate:account.settings.liquidationPenaltyPct/100});account.positions[input.symbol]={tradeId,marketKey,marketType:"futures",symbol:input.symbol,side:input.side,quantity,contractVolume,contractSize:currentContract.contractSize,priceUnit:currentContract.priceUnit,volUnit:currentContract.volUnit,minContractVolume:currentContract.minVol,maxContractVolume:currentContract.maxVol,entryDepthFill,entryPrice:slipped,leverage:sizing.leverage,margin,marginMode,...exits,estimatedLiquidation,entryFee:fee,...feeSnapshot,...fundingPositionSnapshot(funding),fundingPnl:0,lastFundingSettlementAt:Date.parse(timestamp),riskPriceSource,lastRiskPrice:marketPrice,openedAt:timestamp};account.fills.push({orderId,fillId:randomUUID(),tradeId,marketKey,marketType:"futures",idempotencyKey:input.idempotencyKey,userId,symbol:input.symbol,side:input.side,marginMode,leverage:sizing.leverage,price:slipped,quantity,contractVolume,contractSize:currentContract.contractSize,priceUnit:currentContract.priceUnit,volUnit:currentContract.volUnit,minContractVolume:currentContract.minVol,maxContractVolume:currentContract.maxVol,entryDepthFill,notional,marginUsed:margin,stopLoss:exits.stopLoss,takeProfit:exits.takeProfit,estimatedLiquidation,riskPriceSource,entryFee:fee,...feeSnapshot,tradingFee:feeBreakdown.tradingFee,liquidationPenalty:feeBreakdown.liquidationPenalty,fee,timestamp,realisedPnl:0,resultingBalance:account.cashBalance});account.idempotencyKeys.push(input.idempotencyKey);account.fills=account.fills.slice(-500);account.idempotencyKeys=account.idempotencyKeys.slice(-1000);account.updatedAt=timestamp;await writeManualAccount(userId,account);return account})}
function closeAt`
);
