import {readFile,writeFile} from "node:fs/promises";
const path="app/lib/manual-paper.ts",source=await readFile(path,"utf8");
const once=(value,oldText,newText,label)=>{const count=value.split(oldText).length-1;if(count!==1)throw new Error(label+": "+count);return value.replace(oldText,newText)};
const submitMarker='export async function submitManualOrder(';
const helper='function planOppositeDepthReplacement(position:ManualPosition,inputSide:ManualSide,sizingNotional:number,marketPrice:number,contract:MexcContractMetadata,depth:DepthEnvelope){const rules=positionDepthRules(position,contract),openContractVolume=quantizeMexcStep(position.contractVolume??position.quantity/rules.contractSize,rules.volUnit,"nearest");let closePreview:PaperDepthFillEvidence;try{closePreview=simulatePaperMarketDepthFill({side:position.side,opening:false,requestedContractVolume:openContractVolume,openContractVolume,minimumRemainingContractVolume:position.minContractVolume??rules.minVol,referencePrice:marketPrice,contract:rules,depth})}catch(error){return mapDepthExitError(error,rules)}const remaining=closePreview.remainingPositionContractVolume??Math.max(0,openContractVolume-closePreview.filledContractVolume);if(remaining>rules.volUnit*1e-9)return {closePreview,entryDepthFill:null};const requestedOrder=validatedContractOrder(sizingNotional,marketPrice,contract);let entryDepthFill:PaperDepthFillEvidence;try{entryDepthFill=simulatePaperMarketDepthFill({side:inputSide,opening:true,requestedContractVolume:requestedOrder.contractVolume,priorConsumedContractVolume:closePreview.filledContractVolume,referencePrice:marketPrice,contract:rules,depth})}catch(error){return mapDepthReverseEntryError(error,rules)}return {closePreview,entryDepthFill}}\n';
let next=once(source,submitMarker,helper+submitMarker,'replacement helper');
next=once(next,'existing=account.positions[input.symbol];if(existing?.side===input.side&&!account.settings.allowAdding)','existing=account.positions[input.symbol];if(existing?.pendingRiskExit&&existing.side!==input.side)fail("RISK_EXIT_PENDING","side","A triggered risk exit must finish before opening the opposite side.");if(existing?.side===input.side&&!account.settings.allowAdding)','pending order guard');
next=once(next,'if(existing&&existing.side!==input.side&&!input.confirmReverse)fail("REVERSAL_CONFIRMATION_REQUIRED","side","Reversal confirmation required.");let entryDepthFill:PaperDepthFillEvidence|undefined','if(existing&&existing.side!==input.side&&!input.confirmReverse)fail("REVERSAL_CONFIRMATION_REQUIRED","side","Reversal confirmation required.");const oppositeDepthPlan=depth&&existing&&existing.side!==input.side?planOppositeDepthReplacement(existing,input.side,sizing.notional,marketPrice,currentContract,depth):null;if(oppositeDepthPlan&&!oppositeDepthPlan.entryDepthFill){closeWithDepthAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close",existing!.quantity,depth!,currentContract,"reversal");account.idempotencyKeys.push(input.idempotencyKey);account.fills=account.fills.slice(-500);account.idempotencyKeys=account.idempotencyKeys.slice(-1000);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account}let entryDepthFill:PaperDepthFillEvidence|undefined','replacement plan');
next=once(next,'if(depth){const requestedOrder=validatedContractOrder(sizing.notional,marketPrice,currentContract);try{entryDepthFill=simulatePaperMarketDepthFill({side:input.side,requestedContractVolume:requestedOrder.contractVolume,referencePrice:marketPrice,contract:currentContract,depth})}','if(depth){const requestedOrder=validatedContractOrder(sizing.notional,marketPrice,currentContract);try{entryDepthFill=oppositeDepthPlan?.entryDepthFill??simulatePaperMarketDepthFill({side:input.side,requestedContractVolume:requestedOrder.contractVolume,referencePrice:marketPrice,contract:currentContract,depth})}','replacement entry plan');
next=once(next,'if(existing)closeAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close","manual");','if(existing){if(depth&&oppositeDepthPlan?.entryDepthFill)closeWithDepthAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close",existing.quantity,depth,currentContract,"reversal");else closeAt(account,userId,input.symbol,marketPrice,input.idempotencyKey+":close","manual");}','replacement close commit');
next=once(next,'const old=account.positions[symbol];if(!old)fail("NO_POSITION","symbol","No manual position.");const rules=positionDepthRules(old,current)','const old=account.positions[symbol];if(!old)fail("NO_POSITION","symbol","No manual position.");if(old.pendingRiskExit)fail("RISK_EXIT_PENDING","side","A triggered risk exit must finish before reversing the position.");const rules=positionDepthRules(old,current)','depth reverse pending guard');
next=once(next,'const old=account.positions[symbol];if(!old)fail("NO_POSITION","symbol","No manual position.");const rawClosePrice=','const old=account.positions[symbol];if(!old)fail("NO_POSITION","symbol","No manual position.");if(old.pendingRiskExit)fail("RISK_EXIT_PENDING","side","A triggered risk exit must finish before reversing the position.");const rawClosePrice=','legacy reverse pending guard');
await writeFile(path,next);
