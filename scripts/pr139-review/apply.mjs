import { append, replaceRegex } from "./utils.mjs";

await replaceRegex(
  "app/lib/manual-paper.ts",
  /export async function latestPublicFundingHistory\(.*?\}\nfunction fundingPositionSnapshot/s,
  `const fundingHistoryCache=new Map<string,{at:number;value:readonly MexcFundingSettlement[]}>();
export async function latestPublicFundingHistory(symbol:string){if(!/^[A-Z0-9]{1,20}_[A-Z0-9]{1,20}$/.test(symbol))fail("INVALID_SYMBOL","symbol","Invalid market symbol.");const cached=fundingHistoryCache.get(symbol),now=Date.now();if(cached&&now-cached.at<30_000)return cached.value;const response=await fetch(\`https://contract.mexc.com/api/v1/contract/funding_rate/history?symbol=\${encodeURIComponent(symbol)}&page_num=1&page_size=1000\`,{signal:AbortSignal.timeout(5000),cache:"no-store"});if(!response.ok)fail("FUNDING_HISTORY_UNAVAILABLE","funding","Public MEXC funding history is unavailable.");try{const value=parseMexcFundingHistory(await response.json(),symbol);fundingHistoryCache.set(symbol,{at:now,value});return value}catch{return fail("FUNDING_HISTORY_UNAVAILABLE","funding","Public MEXC funding history is unavailable.")}}
function fundingPositionSnapshot`
);

await replaceRegex(
  "app/lib/manual-paper.ts",
  /export async function partialCloseManualPosition\(.*?\}\)\nexport async function reverseManualPosition/s,
  `export async function partialCloseManualPosition(userId:string,symbol:string,key:string,marketPrice:number,input:{percentage?:unknown;quantity?:unknown}){return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,key);const position=account.positions[symbol];if(!position)fail("NO_POSITION","symbol","No manual position.");const closeQuantity=closeQuantityForInput(position,input),rawSlipped=marketPrice*(1+(position.side==="long"?-1:1)*account.settings.slippagePct/100),slipped=position.priceUnit?quantizeMexcExecutionPrice(rawSlipped,position.priceUnit,position.side,false):rawSlipped,notional=closeQuantity*slipped,feeSnapshot=positionMarketTakerFeeSnapshot(position,account.settings),feeBreakdown=paperExecutionFee(notional,feeSnapshot),fee=feeBreakdown.totalFee,pnl=(slipped-position.entryPrice)*closeQuantity*(position.side==="long"?1:-1),ratio=closeQuantity/position.quantity,timestamp=new Date().toISOString(),fullyClosed=ratio>=1-1e-12,allocatedEntryFee=(position.entryFee??0)*ratio,fundingPnl=fullyClosed?(position.fundingPnl??0):0,tradingNet=pnl-allocatedEntryFee-fee,net=tradingNet+fundingPnl;account.cashBalance+=pnl-fee;account.realisedPnl+=tradingNet;account.fees+=fee;account.fills.push({orderId:randomUUID(),fillId:randomUUID(),tradeId:position.tradeId,marketKey:position.marketKey,marketType:position.marketType,idempotencyKey:key,userId,symbol,side:"close",marginMode:position.marginMode,leverage:position.leverage,entryPrice:position.entryPrice,price:slipped,quantity:closeQuantity,...precisionSnapshot(position,position.contractSize?Number((closeQuantity/position.contractSize).toPrecision(15)):undefined),notional,marginUsed:(position.margin??position.quantity*position.entryPrice/position.leverage)*ratio,entryFee:allocatedEntryFee,exitFee:fee,...feeSnapshot,tradingFee:feeBreakdown.tradingFee,liquidationPenalty:feeBreakdown.liquidationPenalty,fee,timestamp,openedAt:position.openedAt,closeReason:fullyClosed?"manual":undefined,grossPnl:pnl,fundingPnl,netPnl:net,realisedPnl:net,resultingBalance:account.cashBalance});if(fullyClosed)delete account.positions[symbol];else{const remainingContractVolume=position.contractVolume===undefined||!position.contractSize?undefined:Number((position.contractVolume-closeQuantity/position.contractSize).toPrecision(15));account.positions[symbol]={...position,...feeSnapshot,quantity:Number((position.quantity-closeQuantity).toPrecision(15)),contractVolume:remainingContractVolume,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)}};account.idempotencyKeys.push(key);account.updatedAt=timestamp;await writeManualAccount(userId,account);return account})}
export async function reverseManualPosition`
);

await replaceRegex(
  "app/lib/manual-paper.ts",
  /export async function reverseManualPosition\(.*?\}\)\n\nexport async function attachManualHistoricalDizyFlow/s,
  `export async function reverseManualPosition(userId:string,symbol:string,key:string,marketPrice:number){return serial(userId,async()=>{const account=await readManualAccount(userId);if(!account.settings.enabled)fail("MANUAL_PAPER_DISABLED","settings.enabled","Manual Paper is disabled.");validateKey(account,key);const old=account.positions[symbol];if(!old)fail("NO_POSITION","symbol","No manual position.");const rawClosePrice=marketPrice*(1+(old.side==="long"?-1:1)*account.settings.slippagePct/100),closePrice=old.priceUnit?quantizeMexcExecutionPrice(rawClosePrice,old.priceUnit,old.side,false):rawClosePrice,openSide:ManualSide=old.side==="long"?"short":"long",rawOpenPrice=marketPrice*(1+(openSide==="long"?1:-1)*account.settings.slippagePct/100),openPrice=old.priceUnit?quantizeMexcExecutionPrice(rawOpenPrice,old.priceUnit,openSide,true):rawOpenPrice,closeNotional=old.quantity*closePrice,openNotional=old.quantity*openPrice,feeSnapshot=positionMarketTakerFeeSnapshot(old,account.settings),closeFeeBreakdown=paperExecutionFee(closeNotional,feeSnapshot),openFeeBreakdown=paperExecutionFee(openNotional,feeSnapshot),closeFee=closeFeeBreakdown.totalFee,openFee=openFeeBreakdown.totalFee,pnl=(closePrice-old.entryPrice)*old.quantity*(old.side==="long"?1:-1),oldEntryFee=old.entryFee??0,oldFundingPnl=old.fundingPnl??0,oldTradingNet=pnl-oldEntryFee-closeFee,oldNet=oldTradingNet+oldFundingPnl,margin=openNotional/old.leverage;if(margin+openFee>manualEquity(account,{[symbol]:marketPrice})-usedManualMargin(account)+(old.margin??old.quantity*old.entryPrice/old.leverage))fail("INSUFFICIENT_EQUITY","amount","Reverse position exceeds available equity.");const timestamp=new Date().toISOString(),tradeId=\`mp1_\${randomUUID().replaceAll("-","")}\`;account.cashBalance+=pnl-closeFee-openFee;account.realisedPnl+=oldTradingNet;account.fees+=closeFee+openFee;account.fills.push({orderId:randomUUID(),fillId:randomUUID(),tradeId:old.tradeId,marketKey:old.marketKey,marketType:old.marketType,idempotencyKey:key+":close",userId,symbol,side:"close",marginMode:old.marginMode,leverage:old.leverage,entryPrice:old.entryPrice,price:closePrice,quantity:old.quantity,...precisionSnapshot(old),notional:closeNotional,marginUsed:old.margin,entryFee:oldEntryFee,exitFee:closeFee,...feeSnapshot,tradingFee:closeFeeBreakdown.tradingFee,liquidationPenalty:closeFeeBreakdown.liquidationPenalty,fee:closeFee,timestamp,openedAt:old.openedAt,closeReason:"reversal",grossPnl:pnl,fundingPnl:oldFundingPnl,netPnl:oldNet,realisedPnl:oldNet,resultingBalance:account.cashBalance+openFee},{orderId:randomUUID(),fillId:randomUUID(),tradeId,marketKey:old.marketKey,marketType:old.marketType,idempotencyKey:key+":open",userId,symbol,side:openSide,marginMode:old.marginMode,leverage:old.leverage,price:openPrice,quantity:old.quantity,...precisionSnapshot(old),notional:openNotional,marginUsed:margin,entryFee:openFee,...feeSnapshot,tradingFee:openFeeBreakdown.tradingFee,liquidationPenalty:openFeeBreakdown.liquidationPenalty,fee:openFee,timestamp,realisedPnl:0,resultingBalance:account.cashBalance});account.positions[symbol]={...old,...feeSnapshot,tradeId,side:openSide,entryPrice:openPrice,entryFee:openFee,margin,stopLoss:null,takeProfit:null,fundingPnl:0,lastFundingSettlementAt:Date.parse(timestamp),openedAt:timestamp};account.idempotencyKeys.push(key);account.updatedAt=timestamp;await writeManualAccount(userId,account);return account})}

export async function attachManualHistoricalDizyFlow`
);

await append(
  "tests/manual-paper-funding.test.mjs",
  `test("funding history requests are cached and use the maximum public page size",async()=>{const {readFile}=await import("node:fs/promises"),source=await readFile(new URL("../app/lib/manual-paper.ts",import.meta.url),"utf8");assert.match(source,/fundingHistoryCache/);assert.match(source,/page_size=1000/);assert.match(source,/now-cached\.at<30_000/)});

test("a 100 percent partial close records entry fee, exit fee and cumulative funding",async()=>{const {mkdtemp,rm}=await import("node:fs/promises"),{tmpdir}=await import("node:os"),{join}=await import("node:path"),{submitManualOrder,syncManualFunding,partialCloseManualPosition}=await import("../app/lib/manual-paper.ts"),prior=process.env.DATA_DIR,root=await mkdtemp(join(tmpdir(),"dizy-paper-funding-full-partial-"));process.env.DATA_DIR=root;try{let account=await submitManualOrder("funding-full-partial",{idempotencyKey:"funding-full-open-001",symbol:"BTC_USDT",side:"long",sizeMode:"fixed-notional",amount:100,leverage:10},100,"fair",contract,parseMexcFundingRate(currentPayload,"BTC_USDT",Date.now()));const opened=Date.parse(account.positions.BTC_USDT.openedAt);await new Promise(resolve=>setTimeout(resolve,5));account=await syncManualFunding("funding-full-partial","BTC_USDT",100,"fair",undefined,[{symbol:"BTC_USDT",fundingRate:.001,settleTime:opened+1,source:"mexc-public-funding-history"}]);const fundingPnl=account.positions.BTC_USDT.fundingPnl;account=await partialCloseManualPosition("funding-full-partial","BTC_USDT","funding-full-close-01",101,{percentage:100});const fill=account.fills.at(-1);assert.equal(fill.closeReason,"manual");assert.equal(fill.fundingPnl,fundingPnl);assert.ok(fill.entryFee>0);assert.ok(fill.exitFee>0);assert.ok(Math.abs(fill.netPnl-((fill.grossPnl??0)-fill.entryFee-fill.exitFee+fundingPnl))<1e-12);assert.equal(account.positions.BTC_USDT,undefined)}finally{if(prior===undefined)delete process.env.DATA_DIR;else process.env.DATA_DIR=prior;await rm(root,{recursive:true,force:true})}});

test("reversal closes the old trade net of entry fee and starts a fresh fee basis",async()=>{const {mkdtemp,rm}=await import("node:fs/promises"),{tmpdir}=await import("node:os"),{join}=await import("node:path"),{submitManualOrder,syncManualFunding,reverseManualPosition}=await import("../app/lib/manual-paper.ts"),prior=process.env.DATA_DIR,root=await mkdtemp(join(tmpdir(),"dizy-paper-funding-reverse-audit-"));process.env.DATA_DIR=root;try{let account=await submitManualOrder("funding-reverse-audit",{idempotencyKey:"funding-reverse-open1",symbol:"BTC_USDT",side:"long",sizeMode:"fixed-notional",amount:100,leverage:10},100,"fair",contract,parseMexcFundingRate(currentPayload,"BTC_USDT",Date.now()));const opened=Date.parse(account.positions.BTC_USDT.openedAt);await new Promise(resolve=>setTimeout(resolve,5));account=await syncManualFunding("funding-reverse-audit","BTC_USDT",100,"fair",undefined,[{symbol:"BTC_USDT",fundingRate:.001,settleTime:opened+1,source:"mexc-public-funding-history"}]);account=await reverseManualPosition("funding-reverse-audit","BTC_USDT","funding-reverse-do-01",101);const [closeFill,openFill]=account.fills.slice(-2),next=account.positions.BTC_USDT;assert.equal(closeFill.closeReason,"reversal");assert.ok(closeFill.entryFee>0);assert.ok(closeFill.exitFee>0);assert.ok(Math.abs(closeFill.netPnl-((closeFill.grossPnl??0)-closeFill.entryFee-closeFill.exitFee+(closeFill.fundingPnl??0)))<1e-12);assert.equal(next.entryFee,openFill.entryFee);assert.equal(next.fundingPnl,0);assert.notEqual(next.tradeId,closeFill.tradeId)}finally{if(prior===undefined)delete process.env.DATA_DIR;else process.env.DATA_DIR=prior;await rm(root,{recursive:true,force:true})}});`
);
