import {readFile,writeFile} from "node:fs/promises";
const replaceOnce=(source,from,to,label)=>{const index=source.indexOf(from);if(index<0)throw new Error(`Missing ${label}`);if(source.indexOf(from,index+from.length)>=0)throw new Error(`Ambiguous ${label}`);return source.slice(0,index)+to+source.slice(index+from.length)};
const transform=(source,start,end,fn,label)=>{const a=source.indexOf(start);if(a<0)throw new Error(`Missing ${label} start`);const b=source.indexOf(end,a+start.length);if(b<0)throw new Error(`Missing ${label} end`);return source.slice(0,a)+fn(source.slice(a,b))+source.slice(b)};
let source=await readFile("app/lib/manual-paper.ts","utf8");

source=transform(source,"function closeWithDepthAt(","\n\nfunction planOppositeDepthReplacement",segment=>{
 segment=replaceOnce(segment,'timestamp=new Date().toISOString(),allocatedEntryFee=(position.entryFee??0)*ratio,fundingPnl=fullyClosed?(position.fundingPnl??0):0,tradingNet=pnl-allocatedEntryFee-fee,net=tradingNet+fundingPnl;account.cashBalance=Math.max(0,account.cashBalance+pnl-fee);account.realisedPnl+=tradingNet;','timestamp=new Date().toISOString(),allocatedMargin=(position.margin??position.quantity*position.entryPrice/position.leverage)*ratio,allocatedEntryFee=(position.entryFee??0)*ratio,fundingPnl=fullyClosed?(position.fundingPnl??0):0,marginSettlement=settleManualClose(account,position,allocatedMargin,pnl-fee),tradingNet=marginSettlement.appliedCashDelta-allocatedEntryFee,net=tradingNet+fundingPnl;account.cashBalance=marginSettlement.cashAfter;account.realisedPnl+=tradingNet;',"depth close settlement");
 segment=replaceOnce(segment,'marginUsed:(position.margin??position.quantity*position.entryPrice/position.leverage)*ratio,entryFee:allocatedEntryFee','marginUsed:allocatedMargin,marginSettlement,entryFee:allocatedEntryFee',"depth close settlement evidence");
 segment=replaceOnce(segment,'if(fullyClosed)delete account.positions[symbol];else account.positions[symbol]=refreshPositionRisk({...position,...feeSnapshot,quantity:Number((remainingContractVolume*rules.contractSize).toPrecision(15)),contractVolume:remainingContractVolume,contractSize:rules.contractSize,priceUnit:rules.priceUnit,volUnit:rules.volUnit,minContractVolume:rules.minVol,maxContractVolume:rules.maxVol,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)},account);return exitDepthFill}','if(fullyClosed)delete account.positions[symbol];else account.positions[symbol]=refreshPositionRisk({...position,...feeSnapshot,quantity:Number((remainingContractVolume*rules.contractSize).toPrecision(15)),contractVolume:remainingContractVolume,contractSize:rules.contractSize,priceUnit:rules.priceUnit,volUnit:rules.volUnit,minContractVolume:rules.minVol,maxContractVolume:rules.maxVol,margin:(position.margin??position.quantity*position.entryPrice/position.leverage)*(1-ratio),entryFee:(position.entryFee??0)*(1-ratio)},account);refreshAccountRisk(account);return exitDepthFill}',"depth close portfolio refresh");
 return segment;
},"closeWithDepthAt");

source=transform(source,"export async function submitManualOrder(","\nfunction closeAt",segment=>{
 segment=replaceOnce(segment,'const currentContract=contract as MexcContractMetadata,equity=manualEquity(account,{[input.symbol]:marketPrice}),sizing=calculateManualSizing','refreshAccountRisk(account);const currentContract=contract as MexcContractMetadata,equity=Math.max(0,account.marginSnapshot?.crossEquity??manualEquity(account,{[input.symbol]:marketPrice})),sizing=calculateManualSizing',"entry sizing equity");
 segment=replaceOnce(segment,'const postCloseEquity=manualEquity(account,{[input.symbol]:marketPrice}),margin=notional/sizing.leverage,available=postCloseEquity-usedManualMargin(account),riskTier=validatedEntryRiskTier','refreshAccountRisk(account);const postCloseEquity=manualEquity(account,{[input.symbol]:marketPrice}),margin=notional/sizing.leverage,available=manualAvailableMargin(account),riskTier=validatedEntryRiskTier',"entry available margin");
 segment=replaceOnce(segment,'account.positions[input.symbol]={tradeId,marketKey,marketType:"futures",symbol:input.symbol,side:input.side,quantity,contractVolume,contractSize:currentContract.contractSize,priceUnit:currentContract.priceUnit,volUnit:currentContract.volUnit,minContractVolume:currentContract.minVol,maxContractVolume:currentContract.maxVol,entryDepthFill,entryPrice:slipped,leverage:sizing.leverage,margin,marginMode,...exits,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,entryFee:fee,...feeSnapshot,...fundingPositionSnapshot(funding),fundingPnl:0,lastFundingSettlementAt:Date.parse(timestamp),riskPriceSource,lastRiskPrice:marketPrice,openedAt:timestamp};account.fills.push','account.positions[input.symbol]={tradeId,marketKey,marketType:"futures",symbol:input.symbol,side:input.side,quantity,contractVolume,contractSize:currentContract.contractSize,priceUnit:currentContract.priceUnit,volUnit:currentContract.volUnit,minContractVolume:currentContract.minVol,maxContractVolume:currentContract.maxVol,entryDepthFill,entryPrice:slipped,leverage:sizing.leverage,margin,marginMode,...exits,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,entryFee:fee,...feeSnapshot,...fundingPositionSnapshot(funding),fundingPnl:0,lastFundingSettlementAt:Date.parse(timestamp),riskPriceSource,lastRiskPrice:marketPrice,openedAt:timestamp};refreshAccountRisk(account);const openedPosition=account.positions[input.symbol];account.fills.push',"entry account refresh");
 segment=replaceOnce(segment,'estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,riskPriceSource,entryFee:fee','estimatedLiquidation:openedPosition.estimatedLiquidation,bankruptcyPrice:openedPosition.bankruptcyPrice,riskTier:openedPosition.riskTier,liquidationAudit:openedPosition.liquidationAudit,marginAudit:openedPosition.marginAudit,riskPriceSource,entryFee:fee',"entry fill margin evidence");
 return segment;
},"submitManualOrder");

source=transform(source,"function closeAt(","\n\nexport async function closeManualPosition",segment=>{
 segment=replaceOnce(segment,'pnl=(slipped-position.entryPrice)*position.quantity*(position.side==="long"?1:-1),tradingNet=pnl-position.entryFee-fee,fundingPnl=position.fundingPnl??0,net=tradingNet+fundingPnl;account.cashBalance=Math.max(0,account.cashBalance+pnl-fee);account.realisedPnl+=tradingNet;','pnl=(slipped-position.entryPrice)*position.quantity*(position.side==="long"?1:-1),allocatedMargin=position.margin,marginSettlement=settleManualClose(account,position,allocatedMargin,pnl-fee),tradingNet=marginSettlement.appliedCashDelta-position.entryFee,fundingPnl=position.fundingPnl??0,net=tradingNet+fundingPnl;account.cashBalance=marginSettlement.cashAfter;account.realisedPnl+=tradingNet;',"full close settlement");
 segment=replaceOnce(segment,'marginUsed:position.margin,stopLoss','marginUsed:allocatedMargin,marginSettlement,stopLoss',"full close settlement evidence");
 segment=replaceOnce(segment,'delete account.positions[symbol]}','delete account.positions[symbol];refreshAccountRisk(account)}',"full close portfolio refresh");
 return segment;
},"closeAt");

source=replaceOnce(source,'export async function syncManualFunding(userId:string,symbol:string,riskPrice:number,source:RiskPriceSource,current?:MexcFundingRateSnapshot,history:readonly MexcFundingSettlement[]=[]){return serial(userId,async()=>{const account=await readManualAccount(userId),position=account.positions[symbol];if(!position)return account;applyFundingHistory(account,userId,position,riskPrice,source,current,history);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}','export async function syncManualFunding(userId:string,symbol:string,riskPrice:number,source:RiskPriceSource,current?:MexcFundingRateSnapshot,history:readonly MexcFundingSettlement[]=[]){return serial(userId,async()=>{const account=await readManualAccount(userId),position=account.positions[symbol];if(!position)return account;applyFundingHistory(account,userId,position,riskPrice,source,current,history);refreshAccountRisk(account);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}',"funding portfolio refresh");

source=transform(source,"export async function markManualPosition(","\n\nfunction validateKey",()=>`export async function markManualPosition(userId:string,symbol:string,riskPrice:number,source:RiskPriceSource,current?:MexcFundingRateSnapshot,history:readonly MexcFundingSettlement[]=[],depth?:DepthEnvelope|null,contract?:MexcContractMetadata|null,requireDepth=false){return serial(userId,async()=>{const account=await readManualAccount(userId);let position=account.positions[symbol];if(!position)return account;applyFundingHistory(account,userId,position,riskPrice,source,current,history);position.lastRiskPrice=riskPrice;position.riskPriceSource=source;refreshAccountRisk(account);position=account.positions[symbol];const evaluated=evaluatePaperClose({side:position.side,riskPrice,stopLoss:position.stopLoss,takeProfit:position.takeProfit,estimatedLiquidation:position.estimatedLiquidation}),reason:ManualRiskExitTrigger["reason"]|null=position.pendingRiskExit?.reason??(evaluated&&isRiskExitReason(evaluated)?evaluated:null);if(reason){if(!position.pendingRiskExit)position.pendingRiskExit={reason,triggeredAt:new Date().toISOString(),triggerPrice:riskPrice,priceSource:source};const reduceOnlyPlan=manualReduceOnlyPlan(position,position.quantity,position.quantity,"risk-exit");if(requireDepth){if(depth&&contract)try{closeWithDepthAt(account,userId,symbol,riskPrice,"auto-"+reason+"-"+Date.now(),position.quantity,depth,contract,reason,reduceOnlyPlan)}catch(error){if(!(error instanceof ManualPaperError&&["DEPTH_STALE","DEPTH_UNAVAILABLE","DEPTH_LIQUIDITY_BELOW_MINIMUM"].includes(error.code)))throw error}}else closeAt(account,userId,symbol,riskPrice,"auto-"+reason+"-"+Date.now(),reason,reduceOnlyPlan)}refreshAccountRisk(account);account.updatedAt=new Date().toISOString();await writeManualAccount(userId,account);return account})}`,"markManualPosition");

source=transform(source,"export async function partialCloseManualPosition(","\n\nasync function reverseManualPositionLegacy",segment=>{
 segment=replaceOnce(segment,'allocatedEntryFee=(position.entryFee??0)*ratio,fundingPnl=fullyClosed?(position.fundingPnl??0):0,tradingNet=pnl-allocatedEntryFee-fee,net=tradingNet+fundingPnl,reduceOnly=finaliseManualReduceOnly(reduceOnlyPlan,requestedQuantity);account.cashBalance+=pnl-fee;account.realisedPnl+=tradingNet;','allocatedMargin=(position.margin??position.quantity*position.entryPrice/position.leverage)*ratio,allocatedEntryFee=(position.entryFee??0)*ratio,fundingPnl=fullyClosed?(position.fundingPnl??0):0,marginSettlement=settleManualClose(account,position,allocatedMargin,pnl-fee),tradingNet=marginSettlement.appliedCashDelta-allocatedEntryFee,net=tradingNet+fundingPnl,reduceOnly=finaliseManualReduceOnly(reduceOnlyPlan,requestedQuantity);account.cashBalance=marginSettlement.cashAfter;account.realisedPnl+=tradingNet;',"partial close settlement");
 segment=replaceOnce(segment,'marginUsed:(position.margin??position.quantity*position.entryPrice/position.leverage)*ratio,entryFee:allocatedEntryFee','marginUsed:allocatedMargin,marginSettlement,entryFee:allocatedEntryFee',"partial close settlement evidence");
 segment=replaceOnce(segment,'}}account.idempotencyKeys.push(key);','}}refreshAccountRisk(account);account.idempotencyKeys.push(key);',"partial close portfolio refresh");
 return segment;
},"partialCloseManualPosition");

source=transform(source,"async function reverseManualPositionLegacy(","\n\nfunction mapDepthReverseEntryError",segment=>{
 segment=replaceOnce(segment,'account.positions[symbol]=refreshPositionRisk({...old,...feeSnapshot,tradeId,side:openSide,entryPrice:openPrice,entryFee:openFee,margin,stopLoss:null,takeProfit:null,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),openedAt:timestamp},account);','const reopened=account.positions[symbol]=refreshPositionRisk({...old,...feeSnapshot,tradeId,side:openSide,entryPrice:openPrice,entryFee:openFee,margin,stopLoss:null,takeProfit:null,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),openedAt:timestamp},account);account.fills[account.fills.length-1]={...account.fills[account.fills.length-1],estimatedLiquidation:reopened.estimatedLiquidation,bankruptcyPrice:reopened.bankruptcyPrice,riskTier:reopened.riskTier,liquidationAudit:reopened.liquidationAudit,marginAudit:reopened.marginAudit};',"legacy reverse opening evidence");
 return segment;
},"reverseManualPositionLegacy");

source=transform(source,"function openReversedDepthPosition(","\nexport async function reverseManualPosition",segment=>{
 segment=replaceOnce(segment,'available=manualEquity(account,{[old.symbol]:marketPrice})-usedManualMargin(account),riskTier','available=manualAvailableMargin(account),riskTier',"depth reverse available margin");
 segment=replaceOnce(segment,'account.positions[old.symbol]={...old,...feeSnapshot,tradeId,side:openSide,quantity:entryDepthFill.quantity,contractVolume:entryDepthFill.filledContractVolume,contractSize:current.contractSize,priceUnit:current.priceUnit,volUnit:current.volUnit,minContractVolume:current.minVol,maxContractVolume:current.maxVol,entryDepthFill,entryPrice:entryDepthFill.executionPrice,entryFee:fee,margin,stopLoss:null,takeProfit:null,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),lastRiskPrice:marketPrice,openedAt:timestamp};account.fills.push','account.positions[old.symbol]={...old,...feeSnapshot,tradeId,side:openSide,quantity:entryDepthFill.quantity,contractVolume:entryDepthFill.filledContractVolume,contractSize:current.contractSize,priceUnit:current.priceUnit,volUnit:current.volUnit,minContractVolume:current.minVol,maxContractVolume:current.maxVol,entryDepthFill,entryPrice:entryDepthFill.executionPrice,entryFee:fee,margin,stopLoss:null,takeProfit:null,estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,fundingPnl:0,pendingRiskExit:undefined,lastFundingSettlementAt:Date.parse(timestamp),lastRiskPrice:marketPrice,openedAt:timestamp};refreshAccountRisk(account);const openedPosition=account.positions[old.symbol];account.fills.push',"depth reverse refresh");
 segment=replaceOnce(segment,'estimatedLiquidation,bankruptcyPrice,riskTier,liquidationAudit,riskPriceSource:old.riskPriceSource','estimatedLiquidation:openedPosition.estimatedLiquidation,bankruptcyPrice:openedPosition.bankruptcyPrice,riskTier:openedPosition.riskTier,liquidationAudit:openedPosition.liquidationAudit,marginAudit:openedPosition.marginAudit,riskPriceSource:old.riskPriceSource',"depth reverse opening evidence");
 return segment;
},"openReversedDepthPosition");

await writeFile("app/lib/manual-paper.ts",source,"utf8");
