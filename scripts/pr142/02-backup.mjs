import {readFile,writeFile} from "node:fs/promises";
const path="app/lib/manual-paper-backup.ts",source=await readFile(path,"utf8");
const replace=(value,oldText,newText,label)=>{const count=value.split(oldText).length-1;if(count!==1)throw new Error(label+": "+count);return value.replace(oldText,newText)};
let next=replace(source,"    availableContractVolume:number(input.availableContractVolume,field+\".availableContractVolume\",0),\n    openPositionContractVolume:","    availableContractVolume:number(input.availableContractVolume,field+\".availableContractVolume\",0),\n    priorConsumedContractVolume:input.priorConsumedContractVolume==null?undefined:number(input.priorConsumedContractVolume,field+\".priorConsumedContractVolume\",0),\n    openPositionContractVolume:","backup field");
next=replace(next,"  if(evidence.filledContractVolume-evidence.requestedContractVolume>volumeTolerance||evidence.filledContractVolume-evidence.availableContractVolume>volumeTolerance)throw new Error(field+\" has impossible filled volume.\");","  if(evidence.filledContractVolume-evidence.requestedContractVolume>volumeTolerance||evidence.filledContractVolume-evidence.availableContractVolume>volumeTolerance)throw new Error(field+\" has impossible filled volume.\");\n  if(evidence.priorConsumedContractVolume!==undefined&&evidence.priorConsumedContractVolume<0)throw new Error(field+\" has invalid prior book consumption.\");","backup validation");
await writeFile(path,next);
